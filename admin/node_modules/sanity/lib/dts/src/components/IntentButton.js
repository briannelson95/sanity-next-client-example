import React from 'react';
import { Button } from '@sanity/ui';
import { IntentLink } from '../router';
/**
 * @beta
 */
export function IntentButton(props) {
    return props.disabled ? (React.createElement(Button, { ...props, as: "a", role: "link", "aria-disabled": "true" })) : (React.createElement(Button, { ...props, as: IntentLink }));
}
//# sourceMappingURL=IntentButton.js.map