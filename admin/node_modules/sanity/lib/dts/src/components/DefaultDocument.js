import React from 'react';
import { Favicons } from './Favicons';
import { GlobalErrorHandler } from './globalErrorHandler';
import { NoJavascript } from './NoJavascript';
const globalStyles = `
  html {
    background-color: #f1f3f6;
  }
  html,
  body,
  #sanity {
    height: 100%;
  }
  body {
    margin: 0;
    -webkit-font-smoothing: antialiased;
  }
`;
const EMPTY_ARRAY = [];
export function DefaultDocument(props) {
    const { entryPath, css = EMPTY_ARRAY, basePath = '/' } = props;
    return (React.createElement("html", { lang: "en" },
        React.createElement("head", null,
            React.createElement("meta", { charSet: "utf-8" }),
            React.createElement("meta", { name: "viewport", content: "width=device-width, initial-scale=1, viewport-fit=cover" }),
            React.createElement("meta", { name: "robots", content: "noindex" }),
            React.createElement("meta", { name: "referrer", content: "same-origin" }),
            React.createElement(Favicons, { basePath: basePath }),
            React.createElement("title", null, "Sanity Studio"),
            React.createElement(GlobalErrorHandler, null),
            css.map((href) => (React.createElement("link", { key: href, rel: "stylesheet", href: href }))),
            React.createElement("style", null, globalStyles)),
        React.createElement("body", null,
            React.createElement("div", { id: "sanity" }),
            React.createElement("script", { type: "module", src: entryPath }),
            React.createElement(NoJavascript, null))));
}
//# sourceMappingURL=DefaultDocument.js.map