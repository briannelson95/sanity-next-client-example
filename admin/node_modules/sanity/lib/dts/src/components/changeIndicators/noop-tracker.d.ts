import React from 'react';
import { Reported } from '../react-track-elements';
declare function NoopTracker({ children }: {
    children: React.ReactNode;
}): JSX.Element;
export declare function createNoopTracker<T>(): {
    useReportedValues: () => Reported<T>[];
    Tracker: typeof NoopTracker;
    useReporter: (id: string | null, value: T | (() => T)) => void;
};
export {};
//# sourceMappingURL=noop-tracker.d.ts.map