import styled, { css } from 'styled-components';
import { Text } from '@sanity/ui';
import React from 'react';
export const TextWithToneStyle = styled(Text)(({ $tone, theme }) => {
    const tone = theme.sanity.color.muted[$tone];
    return css `
      &:not([data-muted]) {
        --card-fg-color: ${tone ? tone.enabled.fg : undefined};
      }

      &[data-dimmed] {
        opacity: 0.3;
      }
    `;
});
export const TextWithTone = React.forwardRef(function TextWithTone(props, ref) {
    const { tone, dimmed, muted, ...rest } = props;
    return (React.createElement(TextWithToneStyle, { "data-ui": "TextWithTone", "data-dimmed": dimmed ? '' : undefined, "data-muted": muted ? '' : undefined, "$tone": tone, muted: muted, ref: ref, ...rest }));
});
//# sourceMappingURL=TextWithTone.js.map