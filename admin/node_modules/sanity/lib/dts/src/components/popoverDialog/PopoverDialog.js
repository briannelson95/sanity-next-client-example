import { CloseIcon } from '@sanity/icons';
import { Box, Button, Card, Container, Flex, Popover, Text } from '@sanity/ui';
import React from 'react';
export function PopoverDialog(props) {
    const { children, header, onClose, width, ...restProps } = props;
    return (React.createElement(Popover, { ...restProps, content: React.createElement(Container, { width: width },
            React.createElement(Flex, { direction: "column" },
                React.createElement(Card, { flex: "none" },
                    React.createElement(Flex, null,
                        React.createElement(Box, { flex: 1 },
                            React.createElement(Text, { weight: "semibold" }, header)),
                        React.createElement(Box, null,
                            React.createElement(Button, { icon: CloseIcon, onClick: onClose })))),
                React.createElement(Card, { flex: 1, overflow: "auto" }, children))) }));
}
//# sourceMappingURL=PopoverDialog.js.map