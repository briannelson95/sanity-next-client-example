import React from 'react';
import styled from 'styled-components';
const DividerDiv = styled.div `
  border-right: 1px solid var(--card-border-color);
  height: auto;

  &[data-hidden] {
    opacity: 0;
  }
`;
export function CollapseMenuDivider(props) {
    const { hidden, ...rest } = props;
    return (React.createElement(DividerDiv, { "data-ui": "CollapseMenuDivider", "data-hidden": hidden ? '' : undefined, ...rest }));
}
//# sourceMappingURL=CollapseMenuDivider.js.map