import { Flex } from '@sanity/ui';
import React, { useEffect, useState } from 'react';
export function ObserveElement(props) {
    const { callback, children, options, ...rest } = props;
    const [el, setEl] = useState(null);
    useEffect(() => {
        if (!el)
            return undefined;
        const io = new IntersectionObserver(callback, options);
        io.observe(el);
        return () => {
            io.unobserve(el);
            io.disconnect();
        };
    }, [el, callback, options]);
    return (React.createElement(Flex, { ref: setEl, ...rest }, children));
}
//# sourceMappingURL=ObserveElement.js.map