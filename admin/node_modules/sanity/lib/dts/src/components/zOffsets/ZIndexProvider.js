import React from 'react';
import { defaults } from './defaults';
import { ZIndexContext } from './ZIndexContext';
/**
 * @todo: Rename to `ZOffsetsProvider`
 *
 * @internal
 */
export function ZIndexProvider({ children }) {
    return React.createElement(ZIndexContext.Provider, { value: defaults }, children);
}
//# sourceMappingURL=ZIndexProvider.js.map