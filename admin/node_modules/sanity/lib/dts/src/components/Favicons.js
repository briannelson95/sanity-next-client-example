import React from 'react';
export function Favicons({ basePath }) {
    const base = `${basePath.replace(/\/+$/, '')}/static`;
    return (React.createElement(React.Fragment, null,
        React.createElement("link", { rel: "icon", href: `${base}/favicon.ico`, sizes: "any" }),
        React.createElement("link", { rel: "icon", href: `${base}/favicon.svg`, type: "image/svg+xml" }),
        React.createElement("link", { rel: "apple-touch-icon", href: `${base}/apple-touch-icon.png` }),
        React.createElement("link", { rel: "manifest", href: `${base}/manifest.webmanifest` })));
}
//# sourceMappingURL=Favicons.js.map