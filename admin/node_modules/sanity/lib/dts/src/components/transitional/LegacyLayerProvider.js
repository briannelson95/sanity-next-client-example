import { LayerProvider } from '@sanity/ui';
import React from 'react';
import { useZIndex } from '../zOffsets/useZIndex';
/**
 * This component should only be used by core Sanity packages.
 * @internal
 */
export function LegacyLayerProvider({ children, zOffset: zOffsetKey, }) {
    const zIndex = useZIndex();
    const zOffset = zIndex[zOffsetKey];
    return React.createElement(LayerProvider, { zOffset: zOffset }, children);
}
//# sourceMappingURL=LegacyLayerProvider.js.map