import React, { createElement } from 'react';
import { isValidElementType } from 'react-is';
export function renderPreviewMedia(value, layout, dimensions) {
    if (isValidElementType(value)) {
        return createElement(value, { layout, dimensions });
    }
    if (typeof value === 'string') {
        return React.createElement("div", null, value);
    }
    return value;
}
export function renderPreviewNode(value, layout, fallbackNode) {
    if (typeof value === 'string') {
        return value;
    }
    if (isValidElementType(value)) {
        return createElement(value, { layout });
    }
    return value || fallbackNode;
}
//# sourceMappingURL=helpers.js.map