import { SanityClient } from '@sanity/client';
import { Observable } from 'rxjs';
import { SanityDocument } from '@sanity/types';
import { DraftsModelDocument, ObservePathsFn, Path, Previewable } from './types';
export declare function create_preview_documentPair(versionedClient: SanityClient, observePaths: ObservePathsFn): {
    observePathsDocumentPair: <T extends SanityDocument = SanityDocument>(value: Previewable, paths: Path[]) => Observable<DraftsModelDocument<T>>;
};
//# sourceMappingURL=documentPair.d.ts.map