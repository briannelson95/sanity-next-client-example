export function getPreviewPaths(preview) {
    const selection = preview?.select;
    if (!selection)
        return undefined;
    return Object.values(selection).map((value) => String(value).split('.'));
}
//# sourceMappingURL=getPreviewPaths.js.map