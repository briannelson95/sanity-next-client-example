import { SchemaType } from '@sanity/types';
import { CSSProperties } from 'react';
import { PreviewProps } from '../../components/previews';
import { Previewable, SortOrdering } from '../types';
export interface SanityPreviewProps extends Omit<PreviewProps, 'value'> {
    ordering?: SortOrdering;
    schemaType: SchemaType;
    value: Previewable;
}
export declare function SanityPreview(props: SanityPreviewProps & {
    style?: CSSProperties;
}): JSX.Element;
//# sourceMappingURL=SanityPreview.d.ts.map