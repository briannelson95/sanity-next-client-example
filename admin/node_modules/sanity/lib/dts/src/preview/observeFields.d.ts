import { Observable } from 'rxjs';
import { SanityClient } from '@sanity/client';
import { CrossProjectTokenStore } from '../datastores/crossProjectToken';
import { ApiConfig, FieldName, Id, ObservePathsFn } from './types';
export declare function create_preview_observeFields(context: {
    crossProjectTokenStore: CrossProjectTokenStore;
    observePaths: ObservePathsFn;
    versionedClient: SanityClient;
}): {
    observeFields: (id: Id, fields: FieldName[], apiConfig?: ApiConfig) => Observable<Record<string, any> | null>;
};
//# sourceMappingURL=observeFields.d.ts.map