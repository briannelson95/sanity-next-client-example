import React, { createContext, useContext, useMemo } from 'react';
const Context = createContext({});
/**
 * unstable
 * @internal
 */
export function useReferenceInputOptions() {
    return useContext(Context);
}
/**
 * unstable
 * @internal
 */
export function ReferenceInputOptionsProvider(props) {
    const { children, activePath, EditReferenceLinkComponent, onEditReference, initialValueTemplateItems, } = props;
    const contextValue = useMemo(() => ({
        activePath,
        EditReferenceLinkComponent,
        onEditReference,
        initialValueTemplateItems,
    }), [activePath, EditReferenceLinkComponent, onEditReference, initialValueTemplateItems]);
    return React.createElement(Context.Provider, { value: contextValue }, children);
}
//# sourceMappingURL=ReferenceInputOptions.js.map