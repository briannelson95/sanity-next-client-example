import React, { memo, useContext } from 'react';
const FormCallbacksContext = React.createContext(null);
export const FormCallbacksProvider = memo(function FormCallbacksProvider(props) {
    return (React.createElement(FormCallbacksContext.Provider, { value: props }, props.children));
});
export function useFormCallbacks() {
    const ctx = useContext(FormCallbacksContext);
    if (!ctx) {
        throw new Error('Form context not provided');
    }
    return ctx;
}
//# sourceMappingURL=FormCallbacks.js.map