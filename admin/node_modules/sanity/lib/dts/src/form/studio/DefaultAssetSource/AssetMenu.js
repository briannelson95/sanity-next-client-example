import React from 'react';
import { LinkIcon, EllipsisVerticalIcon, TrashIcon } from '@sanity/icons';
import { Button, Menu, MenuItem, MenuButton } from '@sanity/ui';
export function AssetMenu({ isSelected, border = true, onAction, }) {
    const triggerButtonMode = isSelected ? 'default' : 'bleed';
    const triggerButtonTone = isSelected ? 'primary' : 'default';
    return (React.createElement(MenuButton, { button: React.createElement(Button, { padding: 2, mode: border ? 'ghost' : triggerButtonMode, icon: EllipsisVerticalIcon, tone: border ? 'default' : triggerButtonTone }), id: "asset-menu", portal: true, menu: React.createElement(Menu, null,
            React.createElement(MenuItem, { text: "Show uses", icon: LinkIcon, onClick: () => {
                    onAction({ type: 'showUsage' });
                } }),
            React.createElement(MenuItem, { text: "Delete", icon: TrashIcon, tone: "critical", onClick: () => {
                    onAction({ type: 'delete' });
                } })), placement: "right" }));
}
//# sourceMappingURL=AssetMenu.js.map