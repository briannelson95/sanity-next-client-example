import React, { useMemo } from 'react';
import { ReviewChangesContext } from './ReviewChangesContext';
/**
 * @internal
 */
export function ReviewChangesContextProvider(props) {
    const { children, changesOpen } = props;
    const contextValue = useMemo(() => ({ changesOpen }), [changesOpen]);
    return (React.createElement(ReviewChangesContext.Provider, { value: contextValue }, children));
}
//# sourceMappingURL=ReviewChangesProvider.js.map