import React from 'react';
import { ImageLoader } from './ImageLoader';
import { ToolCanvas } from './ToolCanvas';
import { Resize } from './Resize';
export function ImageTool(props) {
    return (React.createElement(ImageLoader, { src: props.src }, ({ isLoading, image, error }) => {
        if (isLoading) {
            return React.createElement("div", null, "Loading...");
        }
        if (error) {
            return React.createElement("div", null, error.message);
        }
        if (image) {
            return (React.createElement(Resize, { image: image, maxHeight: ImageTool.maxHeight, maxWidth: ImageTool.maxWidth }, (canvas) => React.createElement(ToolCanvas, { image: canvas, ...props })));
        }
        return null;
    }));
}
ImageTool.maxHeight = 500;
ImageTool.maxWidth = 1000;
//# sourceMappingURL=ImageTool.js.map