import React from 'react';
import { useId } from '@reach/auto-id';
import { Box, Dialog, PortalProvider, usePortal } from '@sanity/ui';
import { PresenceOverlay } from '../../../../../presence';
export function DefaultEditDialog(props) {
    const { onClose, children, title, width = 1 } = props;
    const dialogId = useId();
    const portal = usePortal();
    return (React.createElement(Dialog, { __unstable_autoFocus: true, id: dialogId || '', onClose: onClose, onClickOutside: onClose, header: title, portal: "default", width: width },
        React.createElement(PresenceOverlay, { margins: [0, 0, 1, 0] },
            React.createElement(Box, { padding: 4 },
                React.createElement(PortalProvider, { element: portal.elements?.default }, children)))));
}
//# sourceMappingURL=DefaultEditDialog.js.map