import { Box, Flex, Text } from '@sanity/ui';
import React from 'react';
import { CommentIcon } from '@sanity/icons';
export function renderCustomMarkers(markers) {
    return markers.map((marker) => {
        if (marker.type === 'customMarkerTest') {
            return (React.createElement(Box, { key: `marker-${marker.type}-${JSON.stringify(marker.path)}` },
                React.createElement(Flex, null,
                    React.createElement(Text, { size: 1 },
                        React.createElement(CommentIcon, null),
                        " Two comments"))));
        }
        return null;
    });
}
//# sourceMappingURL=customMarkers.js.map