import React, { createElement, isValidElement } from 'react';
import { isValidElementType } from 'react-is';
import { useId } from '@reach/auto-id';
import { Box, Flex, Text } from '@sanity/ui';
import { FileButton } from './FileInputMenuItem.styled';
export const FileInputMenuItem = React.forwardRef(function FileInputMenuItem(props, forwardedRef) {
    const { icon, id: idProp, accept, capture, fontSize, multiple, onSelect, padding = 3, space = 3, textAlign, text, disabled, ...rest } = props;
    const id = useId(idProp);
    const handleChange = React.useCallback((event) => {
        if (onSelect && event.target.files) {
            onSelect(Array.from(event.target.files));
        }
    }, [onSelect]);
    const content = (React.createElement(Flex, { align: "center", justify: "flex-start", padding: padding },
        icon && (React.createElement(Box, { marginRight: text ? space : undefined },
            React.createElement(Text, { size: fontSize },
                isValidElement(icon) && icon,
                isValidElementType(icon) && createElement(icon)))),
        text && (React.createElement(Text, { align: textAlign, size: fontSize, textOverflow: "ellipsis" }, text))));
    return (React.createElement(FileButton, { ...rest, htmlFor: id, padding: 0, fontSize: 2, disabled: disabled, ref: forwardedRef },
        content,
        React.createElement("input", { "data-testid": "file-button-input", accept: accept, capture: capture, id: id, multiple: multiple, onChange: handleChange, type: "file", value: "", disabled: disabled })));
});
//# sourceMappingURL=FileInputMenuItem.js.map