import React from 'react';
import { Calendar } from './calendar/Calendar';
export const DatePicker = React.forwardRef(function DatePicker(props, ref) {
    const { value = new Date(), onChange, ...rest } = props;
    const [focusedDate, setFocusedDay] = React.useState();
    const handleSelect = React.useCallback((nextDate) => {
        onChange(nextDate);
        setFocusedDay(undefined);
    }, [onChange]);
    return (React.createElement(Calendar, { ...rest, ref: ref, selectedDate: value, onSelect: handleSelect, focusedDate: focusedDate || value, onFocusedDateChange: setFocusedDay }));
});
//# sourceMappingURL=DatePicker.js.map