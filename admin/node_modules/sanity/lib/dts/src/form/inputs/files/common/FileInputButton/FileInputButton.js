import React, { createElement, isValidElement } from 'react';
import { isValidElementType } from 'react-is';
import { useId } from '@reach/auto-id';
import { Box, Flex, Text, useTheme } from '@sanity/ui';
import { FileButton } from './styles';
export const FileInputButton = React.forwardRef(function FileInputButton(props, forwardedRef) {
    const { icon, id: idProp, accept, capture, fontSize, multiple, onSelect, padding = 3, space = 3, textAlign, text, disabled, ...rest } = props;
    const id = useId(idProp);
    const theme = useTheme();
    const handleChange = React.useCallback((event) => {
        if (onSelect && event.target.files) {
            onSelect(Array.from(event.target.files));
        }
    }, [onSelect]);
    const content = (React.createElement(Flex, { align: "center", justify: "center", padding: padding },
        icon && (React.createElement(Box, { marginRight: text ? space : undefined },
            React.createElement(Text, { size: fontSize },
                isValidElement(icon) && icon,
                isValidElementType(icon) && createElement(icon)))),
        text && (React.createElement(Text, { align: textAlign, size: fontSize, textOverflow: "ellipsis", weight: theme.sanity.button.textWeight }, text))));
    return (React.createElement(FileButton, { ...rest, htmlFor: id, padding: 0, fontSize: 2, disabled: disabled },
        content,
        React.createElement("input", { "data-testid": "file-button-input", accept: accept, capture: capture, id: id, multiple: multiple, onChange: handleChange, ref: forwardedRef, type: "file", value: "", disabled: disabled })));
});
//# sourceMappingURL=FileInputButton.js.map