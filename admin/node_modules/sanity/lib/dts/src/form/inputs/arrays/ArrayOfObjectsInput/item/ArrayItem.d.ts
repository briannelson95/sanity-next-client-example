import { Path, SchemaType } from '@sanity/types';
import React from 'react';
import { _ArrayInput_ArrayMember } from '../types';
import { FIXME, RenderPreviewCallback } from '../../../../types';
export interface ArrayItemProps {
    children: React.ReactNode;
    focused?: boolean;
    changed: boolean;
    layout?: 'grid';
    index: number;
    insertableTypes: SchemaType[];
    onClick: () => void;
    onFocus: (event: React.FocusEvent) => void;
    onInsert: (event: {
        items: unknown[];
        position: 'before' | 'after';
    }) => void;
    onRemove: (value: _ArrayInput_ArrayMember) => void;
    open: boolean;
    path: Path;
    presence: FIXME[];
    readOnly?: boolean;
    renderPreview: RenderPreviewCallback;
    schemaType: SchemaType;
    validation: FIXME[];
    value: _ArrayInput_ArrayMember;
}
export declare const ArrayItem: React.NamedExoticComponent<ArrayItemProps>;
//# sourceMappingURL=ArrayItem.d.ts.map