import React, { forwardRef } from 'react';
import { Tab } from '@sanity/ui';
export const GroupTab = forwardRef(function GroupTab(props, ref) {
    // Separate props for resolving conditional hidden groups
    const { onClick } = props;
    // Here goes the content of our component
    const handleClick = React.useCallback(() => {
        onClick?.(props.name);
    }, [props.name, onClick]);
    return (React.createElement(Tab, { "data-testid": `group-tab-${name}`, size: 1, id: `${props.name}-tab`, label: props.title, ref: ref, ...props, onClick: handleClick }));
});
export const GroupOption = (props) => {
    const { name, title, ...rest } = props;
    const { selected } = props;
    return (React.createElement("option", { title: title, value: name, id: `${name}-tab`, "aria-controls": rest['aria-controls'], "data-testid": `group-select-${name}`, "aria-selected": selected ? 'true' : 'false' }, title || name));
};
//# sourceMappingURL=GroupTab.js.map