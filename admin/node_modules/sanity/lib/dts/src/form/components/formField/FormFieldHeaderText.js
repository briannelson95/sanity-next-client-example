/* eslint-disable camelcase */
import { Box, Flex, Stack, Text } from '@sanity/ui';
import React, { memo } from 'react';
import { FormFieldValidationStatus } from './FormFieldValidationStatus';
const EMPTY_ARRAY = [];
export const FormFieldHeaderText = memo(function FormFieldHeaderText(props) {
    const { description, inputId, title, validation = EMPTY_ARRAY } = props;
    const hasValidations = validation.length > 0;
    return (React.createElement(Stack, { space: 2 },
        React.createElement(Flex, null,
            React.createElement(Text, { as: "label", htmlFor: inputId, weight: "semibold", size: 1 }, title || React.createElement("span", { style: { color: 'var(--card-muted-fg-color)' } }, "Untitled")),
            hasValidations && (React.createElement(Box, { marginLeft: 2 },
                React.createElement(FormFieldValidationStatus, { fontSize: 1, validation: validation })))),
        description && (React.createElement(Text, { muted: true, size: 1 }, description))));
});
//# sourceMappingURL=FormFieldHeaderText.js.map