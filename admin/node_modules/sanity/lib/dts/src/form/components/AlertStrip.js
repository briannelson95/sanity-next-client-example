import { ErrorOutlineIcon, WarningOutlineIcon, InfoOutlineIcon } from '@sanity/icons';
import { Box, Card, Flex } from '@sanity/ui';
import React from 'react';
import { Details } from './Details';
const STATUS_TONES = {
    warning: 'caution',
    error: 'critical',
    info: 'positive',
};
const STATUS_ICONS = {
    warning: React.createElement(WarningOutlineIcon, null),
    error: React.createElement(ErrorOutlineIcon, null),
    info: React.createElement(InfoOutlineIcon, null),
};
export function AlertStrip(props) {
    const { children, status = 'warning', title, ...rest } = props;
    return (React.createElement(Card, { radius: 2, tone: STATUS_TONES[status], ...rest, "data-ui": "Alert" },
        React.createElement(Flex, { padding: 1 }, children && (React.createElement(Box, { flex: 1 },
            React.createElement(Details, { icon: STATUS_ICONS[status], title: title },
                React.createElement(Box, { marginLeft: 3, marginTop: 3 }, children)))))));
}
//# sourceMappingURL=AlertStrip.js.map