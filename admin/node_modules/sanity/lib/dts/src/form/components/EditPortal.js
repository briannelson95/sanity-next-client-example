import React from 'react';
import { Box, Dialog, Layer } from '@sanity/ui';
import { PopoverDialog } from '../../components/popoverDialog';
import { PresenceOverlay } from '../../presence';
const PRESENCE_MARGINS = [0, 0, 1, 0];
export function EditPortal(props) {
    const { type, id, onClose, children, legacy_referenceElement: referenceElement, header } = props;
    if (type === 'inline') {
        return React.createElement(React.Fragment, null, children);
    }
    if (type === 'fullscreen') {
        return (React.createElement(Layer, null,
            React.createElement(Dialog, { width: "auto", id: id || '', onClose: onClose, header: header, __unstable_autoFocus: false },
                React.createElement(PresenceOverlay, { margins: PRESENCE_MARGINS },
                    React.createElement(Box, { padding: 4 }, children)))));
    }
    // @ts-expect-error fold is deprecated and should not be visible though types
    if (type === 'popover' || type === 'fold') {
        // @ts-expect-error fold is deprecated and should not be visible though types
        if (type === 'fold') {
            console.warn(`The option named \`editItem: "fold"\` is no longer supported`);
        }
        return (React.createElement(PopoverDialog, { onClose: onClose, referenceElement: referenceElement, placement: "auto", title: header },
            React.createElement(PresenceOverlay, { margins: PRESENCE_MARGINS },
                React.createElement(Box, { padding: 4 }, children))));
    }
    return (React.createElement(Dialog, { width: 1, id: id || '', onClose: onClose, header: header, __unstable_autoFocus: false },
        React.createElement(PresenceOverlay, { margins: PRESENCE_MARGINS },
            React.createElement(Box, { padding: 4 }, children))));
}
//# sourceMappingURL=EditPortal.js.map