/* eslint-disable camelcase */
import React, { memo } from 'react';
import { Box, Flex } from '@sanity/ui';
import { FieldPresence } from '../../../presence';
import { FormFieldHeaderText } from './FormFieldHeaderText';
export const FormFieldHeader = memo(function FormFieldHeader(props) {
    const { validation, __unstable_presence: presence, description, inputId, title } = props;
    return (React.createElement(Flex, { align: "flex-end" },
        React.createElement(Box, { flex: 1, paddingY: 2 },
            React.createElement(FormFieldHeaderText, { validation: validation, description: description, inputId: inputId, title: title })),
        presence && presence.length > 0 && (React.createElement(Box, null,
            React.createElement(FieldPresence, { maxAvatars: 4, presence: presence })))));
});
//# sourceMappingURL=FormFieldHeader.js.map