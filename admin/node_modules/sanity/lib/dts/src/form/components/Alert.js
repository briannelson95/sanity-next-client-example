import { ErrorOutlineIcon, WarningOutlineIcon } from '@sanity/icons';
import { Box, Card, Flex, Text } from '@sanity/ui';
import React from 'react';
import styled from 'styled-components';
const STATUS_TONES = {
    warning: 'caution',
    error: 'critical',
};
const SuffixBox = styled(Box) `
  border-top: 1px solid var(--card-border-color);
`;
export function Alert(props) {
    const { children, status = 'warning', suffix, title, ...rest } = props;
    return (React.createElement(Card, { radius: 2, tone: STATUS_TONES[status], ...rest, "data-ui": "Alert" },
        React.createElement(Flex, { padding: 4 },
            React.createElement(Box, null,
                React.createElement(Text, { size: 1 },
                    status === 'warning' && React.createElement(WarningOutlineIcon, null),
                    status === 'error' && React.createElement(ErrorOutlineIcon, null))),
            React.createElement(Box, { flex: 1, marginLeft: 3 },
                React.createElement(Text, { size: 1, weight: "semibold" }, title),
                children && React.createElement(Box, { marginTop: 3 }, children))),
        suffix && React.createElement(SuffixBox, null, suffix)));
}
//# sourceMappingURL=Alert.js.map