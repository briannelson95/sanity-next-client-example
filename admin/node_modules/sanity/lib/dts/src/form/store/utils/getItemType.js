import { resolveTypeName } from '@sanity/util/content';
export function getItemType(arrayType, item) {
    const itemTypeName = resolveTypeName(item);
    return itemTypeName === 'object' && arrayType.of.length === 1
        ? arrayType.of[0]
        : arrayType.of.find((memberType) => memberType.name === itemTypeName);
}
export function getPrimitiveItemType(arrayType, item) {
    const itemTypeName = resolveTypeName(item);
    return arrayType?.of.find((memberType) => memberType.name === itemTypeName || memberType.jsonType === itemTypeName);
}
//# sourceMappingURL=getItemType.js.map