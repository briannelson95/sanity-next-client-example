import { Subscription } from 'rxjs';
export declare function createSubscriptionManager(...keys: string[]): {
    add: (key: string, subscription: Subscription) => void;
    replace: (key: string, subscription: Subscription) => void;
    unsubscribe: (key: string) => void;
    unsubscribeAll: () => void;
};
//# sourceMappingURL=subscriptionManager.d.ts.map