import React from 'react';
import { PreviewPrimitive } from './PreviewPrimitive';
import { PreviewObject } from './PreviewObject';
import { PreviewArray } from './PreviewArray';
const NO_VALUE = React.createElement(PreviewPrimitive, { value: "<no value>" });
export function PreviewAny(props) {
    const { value, ...rest } = props;
    switch (typeof value) {
        case 'number':
        case 'boolean':
        case 'string': {
            return React.createElement(PreviewPrimitive, { ...rest, value: value });
        }
        case 'undefined': {
            return NO_VALUE;
        }
        case 'object': {
            if (value === null) {
                return NO_VALUE;
            }
            if (Array.isArray(value)) {
                return React.createElement(PreviewArray, { ...rest, value: value });
            }
            return React.createElement(PreviewObject, { ...rest, value: value });
        }
        default: {
            return React.createElement("span", null, '<unknown>');
        }
    }
}
//# sourceMappingURL=PreviewAny.js.map