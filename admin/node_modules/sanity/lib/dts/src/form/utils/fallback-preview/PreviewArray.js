import React from 'react';
import { PreviewAny } from './PreviewAny';
export function PreviewArray(props) {
    const { value, maxDepth = 4, _depth = 0, ...rest } = props;
    if (_depth >= maxDepth) {
        return React.createElement("span", null,
            "Array(",
            value.length,
            ")");
    }
    return (React.createElement("ul", null, value.map((item, i) => {
        return (React.createElement("li", { key: i },
            React.createElement(PreviewAny, { ...rest, value: item, _depth: _depth + 1, maxDepth: maxDepth })));
    })));
}
//# sourceMappingURL=PreviewArray.js.map