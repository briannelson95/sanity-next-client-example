import React from 'react';
import { capitalize } from 'lodash';
import { PreviewAny } from './PreviewAny';
export function PreviewObject(props) {
    const { value, maxDepth = 4, _depth = 0, ...rest } = props;
    const { _type, _key, ...restValue } = value;
    const keys = Object.keys(restValue);
    if (_depth >= maxDepth) {
        return (React.createElement("span", null,
            capitalize(_type || 'object'),
            " ",
            `{${keys.join(', ')}}`));
    }
    return (React.createElement("div", null,
        _type && (React.createElement("h3", null,
            React.createElement("em", null, capitalize(_type)),
            ":")),
        React.createElement("ul", null, keys.map((key) => (React.createElement("li", { key: key },
            React.createElement("b", null, key),
            ":",
            ' ',
            React.createElement(PreviewAny, { ...rest, value: restValue[key], _depth: _depth + 1, maxDepth: maxDepth })))))));
}
//# sourceMappingURL=PreviewObject.js.map