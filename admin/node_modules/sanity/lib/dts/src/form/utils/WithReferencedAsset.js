import React from 'react';
import { useMemoObservable } from 'react-rx';
export function WithReferencedAsset(props) {
    const { reference, children, observeAsset, waitPlaceholder } = props;
    const documentId = reference?._ref;
    const asset = useMemoObservable(() => observeAsset(documentId), [documentId]);
    return React.createElement(React.Fragment, null, documentId && asset ? children(asset) : waitPlaceholder);
}
//# sourceMappingURL=WithReferencedAsset.js.map