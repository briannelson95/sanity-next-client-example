import React, { useMemo } from 'react';
import PortableText from './components/PortableText';
import { createPortableTextDiff } from './helpers';
export const PTDiff = (props) => {
    const { diff, schemaType } = props;
    const ptDiff = useMemo(() => createPortableTextDiff(diff, schemaType), [diff, schemaType]);
    return (React.createElement("div", { "data-diff-action": diff.action },
        React.createElement(PortableText, { diff: ptDiff, schemaType: schemaType })));
};
//# sourceMappingURL=PTDiff.js.map