import { Box, Flex, Text } from '@sanity/ui';
import React from 'react';
import { DiffTooltip, FromToArrow, useDiffAnnotationColor } from '../../../diff';
import { Checkbox, Switch } from '../preview';
export const BooleanFieldDiff = ({ diff, schemaType }) => {
    const { fromValue, toValue } = diff;
    const { title, options } = schemaType;
    const Preview = options?.layout === 'checkbox' ? Checkbox : Switch;
    const userColor = useDiffAnnotationColor(diff, []);
    const showToValue = toValue !== undefined && toValue !== null;
    return (React.createElement(Flex, { align: "center" },
        React.createElement(DiffTooltip, { diff: diff },
            React.createElement(Flex, { align: "center" },
                React.createElement(Preview, { checked: fromValue, color: userColor }),
                showToValue && (React.createElement(React.Fragment, null,
                    React.createElement(Box, { marginX: 2 },
                        React.createElement(FromToArrow, null)),
                    React.createElement(Preview, { checked: toValue, color: userColor }))))),
        showToValue && title && (React.createElement(Box, { marginLeft: 2 },
            React.createElement(Text, { size: 1, weight: "semibold" }, title)))));
};
//# sourceMappingURL=BooleanFieldDiff.js.map