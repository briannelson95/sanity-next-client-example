import { Box } from '@sanity/ui';
import React from 'react';
import styled from 'styled-components';
const NumberWrapper = styled.div `
  display: inline-block;
  word-break: break-all;
`;
export const NumberPreview = (props) => {
    const { value } = props;
    return (React.createElement(Box, { as: NumberWrapper, paddingX: 2, paddingY: 1 }, value));
};
//# sourceMappingURL=NumberPreview.js.map