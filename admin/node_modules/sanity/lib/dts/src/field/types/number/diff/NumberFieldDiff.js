import React from 'react';
import { DiffFromTo } from '../../../diff';
import { NumberPreview } from '../preview/NumberPreview';
export const NumberFieldDiff = ({ diff, schemaType }) => {
    return (React.createElement(DiffFromTo, { diff: diff, schemaType: schemaType, previewComponent: NumberPreview, layout: "inline" }));
};
//# sourceMappingURL=NumberFieldDiff.js.map