import React from 'react';
import { DiffFromTo } from '../../../diff';
import { SlugPreview } from '../preview';
export const SlugFieldDiff = ({ diff, schemaType }) => {
    return (React.createElement(DiffFromTo, { layout: "inline", diff: diff, schemaType: schemaType, previewComponent: SlugPreview }));
};
//# sourceMappingURL=SlugFieldDiff.js.map