import React from 'react';
import styled from 'styled-components';
import { DiffFromTo, DiffString } from '../../../diff';
import { StringPreview } from '../preview/StringPreview';
const StringWrapper = styled.div `
  white-space: pre-wrap;
  word-wrap: break-word;
`;
export const StringFieldDiff = ({ diff, schemaType }) => {
    const { options } = schemaType;
    if (options?.list) {
        // When the string is considered to be an "enum", don't show individual
        // string segment changes, rather treat is as a "from -> to" diff
        return React.createElement(DiffFromTo, { diff: diff, previewComponent: StringPreview, schemaType: schemaType });
    }
    return (React.createElement(StringWrapper, null,
        React.createElement(DiffString, { diff: diff })));
};
//# sourceMappingURL=StringFieldDiff.js.map