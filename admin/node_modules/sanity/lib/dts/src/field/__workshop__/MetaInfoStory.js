import { ImageIcon } from '@sanity/icons';
import { Box, Card } from '@sanity/ui';
import React from 'react';
import { MetaInfo } from '../diff';
export default function MetaInfoStory() {
    return (React.createElement(Box, { padding: 4 },
        React.createElement(MetaInfo, { action: "Action", icon: ImageIcon, markRemoved: true, title: "Title" },
            React.createElement(Card, { tone: "transparent" }, "(children)"))));
}
//# sourceMappingURL=MetaInfoStory.js.map