import { Box, Button } from '@sanity/ui';
import React, { useCallback, useState } from 'react';
import { DiffErrorBoundary } from '../diff/components/DiffErrorBoundary';
export default function DiffErrorBoundaryStory() {
    return (React.createElement(Box, { padding: 4 },
        React.createElement(DiffErrorBoundary, null,
            React.createElement(Test, null))));
}
function Test() {
    const [error, setError] = useState('');
    const handleClick = useCallback(() => {
        setError('throw');
    }, []);
    if (error) {
        throw new Error('Throw!');
    }
    return React.createElement(Button, { onClick: handleClick, text: "Throw" });
}
//# sourceMappingURL=DiffErrorBoundaryStory.js.map