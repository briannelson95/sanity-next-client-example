import { BoxProps } from '@sanity/ui';
import React from 'react';
import { DefaultTheme, StyledComponent } from 'styled-components';
import { FieldChangeNode } from '../../types';
export interface DiffInspectWrapperProps {
    children: React.ReactNode;
    change: FieldChangeNode;
    as?: StyledComponent<'div', DefaultTheme>;
}
export declare function DiffInspectWrapper(props: DiffInspectWrapperProps & BoxProps): React.ReactElement;
//# sourceMappingURL=DiffInspectWrapper.d.ts.map