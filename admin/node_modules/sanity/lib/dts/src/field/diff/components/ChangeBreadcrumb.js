import React from 'react';
import { Breadcrumbs, Text } from '@sanity/ui';
import { ChangeTitleSegment } from './ChangeTitleSegment';
export function ChangeBreadcrumb(props) {
    const { change, titlePath } = props;
    return (React.createElement(Breadcrumbs, { separator: React.createElement(Text, { muted: true, size: 1 }, "/") }, titlePath.map((titleSegment, idx) => {
        const showSegment = typeof titleSegment === 'string' || !change || change.showIndex;
        if (!showSegment) {
            return null;
        }
        return React.createElement(ChangeTitleSegment, { change: change, key: idx, segment: titleSegment });
    })));
}
//# sourceMappingURL=ChangeBreadcrumb.js.map