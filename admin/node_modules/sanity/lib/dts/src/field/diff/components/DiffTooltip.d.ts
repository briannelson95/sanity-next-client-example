import { Path } from '@sanity/types';
import React from 'react';
import { TooltipProps } from '@sanity/ui';
import { AnnotationDetails, Diff } from '../../types';
export interface DiffTooltipProps extends TooltipProps {
    children: React.ReactElement;
    description?: React.ReactNode;
    diff: Diff;
    path?: Path | string;
}
export interface DiffTooltipWithAnnotationsProps extends TooltipProps {
    annotations: AnnotationDetails[];
    children: React.ReactElement;
    description?: React.ReactNode;
}
export declare function DiffTooltip(props: DiffTooltipProps | DiffTooltipWithAnnotationsProps): JSX.Element;
//# sourceMappingURL=DiffTooltip.d.ts.map