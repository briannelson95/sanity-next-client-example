import { Text } from '@sanity/ui';
import React, { createElement } from 'react';
import { ArrowRightIcon, ArrowDownIcon } from '@sanity/icons';
const arrowComponents = {
    down: ArrowDownIcon,
    right: ArrowRightIcon,
};
export function FromToArrow(props) {
    const { direction = 'right', ...restProps } = props;
    const arrowComponent = arrowComponents[direction];
    return (React.createElement(Text, { muted: true, size: 1, ...restProps }, createElement(arrowComponent)));
}
//# sourceMappingURL=FromToArrow.js.map