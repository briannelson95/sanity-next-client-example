import React from 'react';
import { Box } from '@sanity/ui';
import { SanityPreview } from '../../../preview';
import { DiffFromTo } from './DiffFromTo';
const FallbackPreview = ({ value, schemaType }) => {
    return (React.createElement(Box, { padding: 2 },
        React.createElement(SanityPreview, { schemaType: schemaType, value: value, layout: "default" })));
};
export const FallbackDiff = (props) => {
    const { diff, schemaType } = props;
    return (React.createElement(DiffFromTo, { diff: diff, schemaType: schemaType, previewComponent: FallbackPreview, layout: "grid" }));
};
//# sourceMappingURL=FallbackDiff.js.map