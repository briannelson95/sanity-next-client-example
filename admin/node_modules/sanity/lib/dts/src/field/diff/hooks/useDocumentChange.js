import { useContext } from 'react';
import { DocumentChangeContext, } from '../contexts/DocumentChangeContext';
export function useDocumentChange() {
    const documentChange = useContext(DocumentChangeContext);
    if (!documentChange) {
        throw new Error('DocumentChange: missing context value');
    }
    return documentChange;
}
//# sourceMappingURL=useDocumentChange.js.map