import { Text, Box, Flex, Stack } from '@sanity/ui';
import React, { createElement } from 'react';
import styled from 'styled-components';
const MetaText = styled(Text) `
  color: inherit;
`;
export function MetaInfo(props) {
    const { title, action, icon, children, markRemoved } = props;
    return (React.createElement(Flex, { padding: 2, align: "center" },
        icon && (React.createElement(Box, { padding: 2 },
            React.createElement(MetaText, { size: 4, forwardedAs: markRemoved ? 'del' : 'div' }, createElement(icon)))),
        React.createElement(Stack, { space: 2, paddingLeft: 2 },
            React.createElement(MetaText, { size: 1, weight: "semibold", forwardedAs: markRemoved ? 'del' : 'h3', textOverflow: "ellipsis" }, title),
            action && React.createElement("div", null, action),
            React.createElement(MetaText, { size: 0, textOverflow: "ellipsis" }, children))));
}
//# sourceMappingURL=MetaInfo.js.map