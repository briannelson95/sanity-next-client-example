import { Workspace } from '../../config';
/**
 * Note: Don't run this on the main thread, use it a forked process
 */
export declare function getStudioConfig(options: {
    configPath?: string;
    basePath: string;
}): Promise<Workspace[]>;
//# sourceMappingURL=getStudioConfig.d.ts.map