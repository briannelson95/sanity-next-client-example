import { ArrowLeftIcon } from '@sanity/icons';
import { Button } from '@sanity/ui';
import React, { memo, useMemo } from 'react';
import { BackLink, PaneHeader, PaneHeaderActions } from '../../components';
import { useDeskTool } from '../../useDeskTool';
export const DocumentListPaneHeader = memo(({ index, initialValueTemplates = [], menuItems = [], menuItemGroups = [], setLayout, setSortOrder, title, }) => {
    const { features } = useDeskTool();
    const actionHandlers = useMemo(() => {
        return {
            setLayout: ({ layout: value }) => {
                setLayout(value);
            },
            setSortOrder: (sort) => {
                setSortOrder(sort);
            },
        };
    }, [setLayout, setSortOrder]);
    return (React.createElement(PaneHeader, { backButton: features.backButton &&
            index > 0 && React.createElement(Button, { as: BackLink, "data-as": "a", icon: ArrowLeftIcon, mode: "bleed" }), title: title, actions: React.createElement(PaneHeaderActions, { initialValueTemplateItems: initialValueTemplates, actionHandlers: actionHandlers, menuItemGroups: menuItemGroups, menuItems: menuItems }) }));
});
DocumentListPaneHeader.displayName = 'DocumentListPaneHeader';
//# sourceMappingURL=DocumentListPaneHeader.js.map