import { Box } from '@sanity/ui';
import React from 'react';
import styled from 'styled-components';
import { usePane } from '../../components';
const Root = styled(Box) `
  position: relative;
`;
export function UserComponentPaneContent(props) {
    const { children } = props;
    const { collapsed } = usePane();
    return (React.createElement(Root, { hidden: collapsed, height: "fill", overflow: "auto" }, children));
}
//# sourceMappingURL=UserComponentPaneContent.js.map