import { SanityClient } from '@sanity/client';
import { Observable } from 'rxjs';
import { SanityDocument } from '@sanity/types';
export declare const getQueryResults: (receivedProps$: Observable<{
    client: SanityClient;
    query: string;
    params: Record<string, any>;
}>, options?: {}) => Observable<{
    result: null;
    error: boolean;
} | {
    result: {
        documents: SanityDocument[];
    };
    loading: boolean;
    error: boolean;
} | {
    result: null;
    loading: boolean;
    error: Error;
} | {
    loading: boolean;
}>;
//# sourceMappingURL=getQueryResults.d.ts.map