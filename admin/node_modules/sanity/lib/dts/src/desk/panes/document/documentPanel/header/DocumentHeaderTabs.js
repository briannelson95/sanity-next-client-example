import React, { useCallback } from 'react';
import { Tab, TabList } from '@sanity/ui';
import { useDocumentPane } from '../../useDocumentPane';
import { usePaneRouter } from '../../../../components';
export function DocumentHeaderTabs() {
    const { activeViewId, paneKey, views } = useDocumentPane();
    const tabPanelId = `${paneKey}tabpanel`;
    return (React.createElement(TabList, { space: 1 }, views.map((view, index) => (React.createElement(DocumentHeaderTab, { icon: view.icon, id: `${paneKey}tab-${view.id}`, isActive: activeViewId === view.id, key: view.id, label: React.createElement(React.Fragment, null, view.title), tabPanelId: tabPanelId, viewId: index === 0 ? null : view.id ?? null })))));
}
function DocumentHeaderTab(props) {
    const { icon, id, isActive, label, tabPanelId, viewId } = props;
    const { ready } = useDocumentPane();
    const { setView } = usePaneRouter();
    const handleClick = useCallback(() => setView(viewId), [setView, viewId]);
    return (React.createElement(Tab, { "aria-controls": tabPanelId, disabled: !ready, fontSize: 1, icon: icon, id: id, selected: isActive, label: label, onClick: handleClick }));
}
//# sourceMappingURL=DocumentHeaderTabs.js.map