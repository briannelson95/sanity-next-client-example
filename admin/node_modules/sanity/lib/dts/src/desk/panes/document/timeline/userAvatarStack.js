import React from 'react';
import { AvatarStack } from '@sanity/ui';
import { UserAvatar } from '../../../../components/UserAvatar';
export function UserAvatarStack({ maxLength, userIds }) {
    return (React.createElement(AvatarStack, { maxLength: maxLength }, userIds.map((userId) => (React.createElement(UserAvatar, { key: userId, user: userId, withTooltip: true })))));
}
//# sourceMappingURL=userAvatarStack.js.map