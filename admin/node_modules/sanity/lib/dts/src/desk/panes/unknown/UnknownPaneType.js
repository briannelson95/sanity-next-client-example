import { Box, Text } from '@sanity/ui';
import React from 'react';
import { isRecord } from '../../../util';
import { Pane, PaneContent, PaneHeader } from '../../components/pane';
/**
 * @internal
 */
export function UnknownPane(props) {
    const { isSelected, pane, paneKey } = props;
    const type = (isRecord(pane) && pane.type) || null;
    return (React.createElement(Pane, { id: paneKey, selected: isSelected },
        React.createElement(PaneHeader, { title: "Unknown pane type" }),
        React.createElement(PaneContent, null,
            React.createElement(Box, { padding: 4 }, typeof type === 'string' ? (React.createElement(Text, { as: "p", muted: true },
                "Structure item of type ",
                React.createElement("code", null, type),
                " is not a known entity.")) : (React.createElement(Text, { as: "p", muted: true },
                "Structure item is missing required ",
                React.createElement("code", null, "type"),
                " property."))))));
}
//# sourceMappingURL=UnknownPaneType.js.map