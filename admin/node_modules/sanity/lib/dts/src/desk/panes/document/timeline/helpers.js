import { TIMELINE_ICON_COMPONENTS, TIMELINE_LABELS } from './constants';
export function formatTimelineEventLabel(type) {
    return TIMELINE_LABELS[type];
}
export function getTimelineEventIconComponent(type) {
    return TIMELINE_ICON_COMPONENTS[type];
}
export function sinceTimelineProps(since, rev) {
    return {
        topSelection: rev,
        bottomSelection: since,
        disabledBeforeSelection: true,
    };
}
export function revTimelineProps(rev) {
    return {
        topSelection: rev,
        bottomSelection: rev,
    };
}
//# sourceMappingURL=helpers.js.map