import React from 'react';
import { ArrowLeftIcon } from '@sanity/icons';
import { Button } from '@sanity/ui';
import { BackLink, PaneHeader, PaneHeaderActions } from '../../components';
import { useDeskTool } from '../../useDeskTool';
export function UserComponentPaneHeader(props) {
    const { actionHandlers, index, menuItems, menuItemGroups, title } = props;
    const { features } = useDeskTool();
    if (!menuItems?.length && !title) {
        return null;
    }
    return (React.createElement(PaneHeader, { actions: React.createElement(PaneHeaderActions, { menuItems: menuItems, menuItemGroups: menuItemGroups, actionHandlers: actionHandlers }), backButton: features.backButton &&
            index > 0 && React.createElement(Button, { as: BackLink, "data-as": "a", icon: ArrowLeftIcon, mode: "bleed" }), title: title }));
}
//# sourceMappingURL=UserComponentPaneHeader.js.map