import { Container, Flex } from '@sanity/ui';
import React, { useMemo } from 'react';
import { DeskToolProvider } from '../../../../DeskToolProvider';
import { DocumentPaneProvider } from '../../DocumentPaneProvider';
import { DocumentStatusBarActions } from '../DocumentStatusBarActions';
export default function DocumentActionsStory() {
    const pane = useMemo(() => ({
        type: 'document',
        id: 'grrm',
        title: 'grrm',
        options: {
            type: 'author',
            id: 'grrm',
        },
    }), []);
    return (React.createElement(Flex, { align: "center", height: "fill", justify: "center", padding: 4, sizing: "border" },
        React.createElement(Container, { width: 0 },
            React.createElement(DeskToolProvider, null,
                React.createElement(DocumentPaneProvider, { index: 1, itemId: "test", pane: pane, paneKey: "test" },
                    React.createElement(DocumentStatusBarActions, null))))));
}
//# sourceMappingURL=DocumentActionsStory.js.map