import React from 'react';
import { Box } from '@sanity/ui';
import { Pane, PaneContent, PaneHeader } from '../../components/pane';
/**
 * @internal
 */
export function ErrorPane(props) {
    const { children, flex, minWidth, paneKey, title = 'Error', tone = 'critical' } = props;
    return (React.createElement(Pane, { flex: flex, id: paneKey, minWidth: minWidth, tone: tone },
        React.createElement(PaneHeader, { title: title }),
        React.createElement(PaneContent, { overflow: "auto" },
            React.createElement(Box, { paddingX: 4, paddingY: 5 }, children))));
}
//# sourceMappingURL=ErrorPane.js.map