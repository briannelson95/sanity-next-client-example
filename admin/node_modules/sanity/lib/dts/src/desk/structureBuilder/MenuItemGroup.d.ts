import { SerializeOptions, Serializable, SerializePath } from './StructureNodes';
import { StructureContext } from './types';
export declare function maybeSerializeMenuItemGroup(item: MenuItemGroup | MenuItemGroupBuilder, index: number, path: SerializePath): MenuItemGroup;
export interface MenuItemGroup {
    id: string;
    title: string;
}
export declare class MenuItemGroupBuilder implements Serializable<MenuItemGroup> {
    protected _context: StructureContext;
    protected _id: string;
    protected _title: string;
    constructor(_context: StructureContext, spec?: MenuItemGroup);
    id(id: string): MenuItemGroupBuilder;
    getId(): string;
    title(title: string): MenuItemGroupBuilder;
    getTitle(): string;
    serialize(options?: SerializeOptions): MenuItemGroup;
}
//# sourceMappingURL=MenuItemGroup.d.ts.map