const actionIds = new WeakMap();
let counter = 0;
export function getHookId(actionHook) {
    const cachedId = actionIds.get(actionHook);
    if (cachedId)
        return cachedId;
    const id = `${actionHook.name || actionHook.displayName || '<anonymous>'}-${counter++}`;
    actionIds.set(actionHook, id);
    return id;
}
//# sourceMappingURL=actionId.js.map