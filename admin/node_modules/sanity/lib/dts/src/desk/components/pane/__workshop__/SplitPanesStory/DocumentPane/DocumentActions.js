import { ChevronDownIcon, PublishIcon } from '@sanity/icons';
import { Button, Flex, Menu, MenuButton, MenuItem, Stack, useToast, } from '@sanity/ui';
import React, { useCallback } from 'react';
const MENU_BUTTON_POPOVER_PROPS = {
    constrainSize: true,
    portal: true,
};
export function DocumentActions() {
    const { push: pushToast } = useToast();
    const publish = useCallback(() => pushToast({ status: 'success', title: 'Successfully published' }), [pushToast]);
    return (React.createElement(Flex, { gap: 1 },
        React.createElement(Stack, { flex: 1 },
            React.createElement(Button, { icon: PublishIcon, onClick: publish, text: "Publish", tone: "positive" })),
        React.createElement(MenuButton, { button: React.createElement(Button, { icon: ChevronDownIcon, mode: "ghost" }), id: "actions", menu: React.createElement(Menu, null,
                React.createElement(MenuItem, { text: "Discard changes" })), popover: MENU_BUTTON_POPOVER_PROPS })));
}
//# sourceMappingURL=DocumentActions.js.map