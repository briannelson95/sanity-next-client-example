import { Box } from '@sanity/ui';
import React, { forwardRef } from 'react';
import { LegacyLayerProvider } from '../../../components/transitional';
import { usePane } from './usePane';
import { Root, RootCard } from './PaneFooter.styles';
/**
 * @beta This API will change. DO NOT USE IN PRODUCTION.
 */
export const PaneFooter = forwardRef(function PaneFooter(props, ref) {
    const { children, padding } = props;
    const { collapsed } = usePane();
    return (React.createElement(LegacyLayerProvider, { zOffset: "paneFooter" },
        React.createElement(Root, { "data-testid": "pane-footer", hidden: collapsed, ref: ref },
            React.createElement(RootCard, { tone: "inherit" },
                React.createElement(Box, { padding: padding }, children)))));
});
//# sourceMappingURL=PaneFooter.js.map