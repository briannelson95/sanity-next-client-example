import { Tooltip } from '@sanity/ui';
import React from 'react';
import { InsufficientPermissionsMessage } from '../../../components/InsufficientPermissionsMessage';
import { useCurrentUser } from '../../../datastores';
export function InsufficientPermissionsMessageTooltip({ reveal, loading, children, }) {
    const currentUser = useCurrentUser();
    if (!reveal) {
        return React.createElement(React.Fragment, null, children);
    }
    return (React.createElement(Tooltip, { content: loading ? 'Loading…' : React.createElement(InsufficientPermissionsMessage, { currentUser: currentUser }), portal: true },
        React.createElement("div", null, children)));
}
//# sourceMappingURL=InsufficientPermissionsMessageTooltip.js.map