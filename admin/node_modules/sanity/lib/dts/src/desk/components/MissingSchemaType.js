import { WarningOutlineIcon } from '@sanity/icons';
import React from 'react';
import { SanityDefaultPreview } from '../../preview';
const getUnknownTypeFallback = (id, typeName) => ({
    title: (React.createElement("em", null,
        "No schema found for type ",
        React.createElement("code", null, typeName))),
    subtitle: (React.createElement("em", null,
        "Document: ",
        React.createElement("code", null, id))),
    media: WarningOutlineIcon,
});
export function MissingSchemaType(props) {
    const { layout, value } = props;
    return (React.createElement(SanityDefaultPreview, { ...getUnknownTypeFallback(value._id, value._type), layout: layout }));
}
//# sourceMappingURL=MissingSchemaType.js.map