import { Box, Card, Flex, Text, Layer, useTheme } from '@sanity/ui';
import React, { useCallback } from 'react';
import styled from 'styled-components';
import { LegacyLayerProvider } from '../../../../../components/transitional';
const RootCard = styled(Card) `
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
`;
const features = {
    inverted: true,
};
export function Navbar(props) {
    const { path, setPath } = props;
    const theme = useTheme();
    const { dark } = theme.sanity.color;
    const handleHomeClick = useCallback(() => {
        setPath(path.slice(0, 1));
    }, [path, setPath]);
    return (React.createElement(LegacyLayerProvider, { zOffset: "navbar" },
        React.createElement(Layer, null,
            React.createElement(RootCard, { scheme: features.inverted ? 'dark' : undefined, shadow: features.inverted ? (dark ? 1 : 0) : 1 },
                React.createElement(Flex, null,
                    React.createElement(Box, { padding: 2 },
                        React.createElement(Card, { as: "button", onClick: handleHomeClick, padding: 3, radius: 2 },
                            React.createElement(Text, { weight: "bold" }, "Studio"))))))));
}
//# sourceMappingURL=Navbar.js.map