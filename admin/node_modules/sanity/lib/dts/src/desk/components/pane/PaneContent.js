import React, { forwardRef } from 'react';
import { usePane } from './usePane';
import { usePaneLayout } from './usePaneLayout';
import { Root } from './PaneContent.styles';
/**
 * @beta This API will change. DO NOT USE IN PRODUCTION.
 */
export const PaneContent = forwardRef(function PaneContent(props, ref) {
    const { as, children, overflow, padding, ...restProps } = props;
    const { collapsed } = usePane();
    const { collapsed: layoutCollapsed } = usePaneLayout();
    return (React.createElement(Root, { "data-testid": "pane-content", forwardedAs: as, ...restProps, flex: 1, hidden: collapsed, overflow: layoutCollapsed ? undefined : overflow, padding: padding, ref: ref, tone: "inherit" }, children));
});
//# sourceMappingURL=PaneContent.js.map