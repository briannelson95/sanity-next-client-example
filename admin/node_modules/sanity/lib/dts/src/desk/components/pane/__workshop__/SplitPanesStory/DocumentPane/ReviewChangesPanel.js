import { CloseIcon } from '@sanity/icons';
import { Button, Flex } from '@sanity/ui';
import React from 'react';
import { PaneHeader } from '../../../PaneHeader';
import { usePane } from '../../../usePane';
export function ReviewChangesPanel(props) {
    const { collapsed } = usePane();
    return (React.createElement(Flex, { direction: "column", flex: 1, hidden: collapsed, style: {
            borderLeft: '1px dashed var(--card-border-color)',
            overflow: 'hidden',
            minWidth: 320,
        } },
        React.createElement(PaneHeader, { actions: React.createElement(Button, { icon: CloseIcon, mode: "bleed", onClick: props.onClose }), title: "Review changes" })));
}
//# sourceMappingURL=ReviewChangesPanel.js.map