import React from 'react';
import { PublishIcon } from '@sanity/icons';
import { Box, Text, Tooltip } from '@sanity/ui';
import { TextWithTone } from '../../components/TextWithTone';
import { TimeAgo } from './TimeAgo';
export function PublishedStatus(props) {
    const { document } = props;
    const updatedAt = document && '_updatedAt' in document && document._updatedAt;
    return (React.createElement(Tooltip, { portal: true, content: React.createElement(Box, { padding: 2 },
            React.createElement(Text, { size: 1 }, document ? (React.createElement(React.Fragment, null,
                "Published ",
                updatedAt && React.createElement(TimeAgo, { time: updatedAt }))) : (React.createElement(React.Fragment, null, "Not published")))) },
        React.createElement(TextWithTone, { tone: "positive", dimmed: !document, muted: !document, size: 1 },
            React.createElement(PublishIcon, null))));
}
//# sourceMappingURL=PublishedStatus.js.map