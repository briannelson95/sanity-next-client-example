import { Button } from '@sanity/ui';
import React, { forwardRef, useMemo } from 'react';
import { IntentLink } from '../../router';
export const IntentButton = forwardRef(function IntentButton(props, ref) {
    const { intent, ...restProps } = props;
    const Link = useMemo(() => 
    // eslint-disable-next-line @typescript-eslint/no-shadow
    forwardRef(function Link(linkProps, linkRef) {
        return (React.createElement(IntentLink, { ...linkProps, intent: intent.type, params: intent.params, ref: linkRef }));
    }), [intent]);
    return props.disabled ? (React.createElement(Button, { ...props, as: "a", role: "link", "aria-disabled": "true" })) : (React.createElement(Button, { ...restProps, as: Link, "data-as": "a", ref: ref }));
});
//# sourceMappingURL=IntentButton.js.map