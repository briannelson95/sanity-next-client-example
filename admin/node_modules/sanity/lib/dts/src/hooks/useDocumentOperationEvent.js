import { useMemoObservable } from 'react-rx';
import { useDocumentStore } from '../datastores';
export function useDocumentOperationEvent(publishedDocId, docTypeName) {
    const documentStore = useDocumentStore();
    return useMemoObservable(() => documentStore.pair.operationEvents(publishedDocId, docTypeName), [publishedDocId, docTypeName]);
}
//# sourceMappingURL=useDocumentOperationEvent.js.map