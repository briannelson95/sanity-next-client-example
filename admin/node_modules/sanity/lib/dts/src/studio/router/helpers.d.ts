import { Tool } from '../../config';
import { RouterState, Router } from '../../router';
import { RouterEvent, RouterStateEvent } from './types';
export declare function resolveDefaultState(tools: Tool[], state: Record<string, unknown> | null): RouterState | null;
export declare function resolveIntentState(tools: Tool[], currentState: RouterState | null, intentState: RouterState): RouterEvent;
export declare function decodeUrlState(rootRouter: Router, pathname: string): RouterStateEvent;
export declare function isNonNullable<T>(value: T): value is NonNullable<T>;
//# sourceMappingURL=helpers.d.ts.map