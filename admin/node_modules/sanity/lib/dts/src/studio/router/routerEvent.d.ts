import { Observable } from 'rxjs';
import { History } from 'history';
import { Tool } from '../../config';
import { Router } from '../../router';
import { RouterEvent } from './types';
interface RouterEventStreamOptions {
    unstable_history: History;
    router: Router;
    tools: Tool[];
}
/**
 * @internal
 */
export declare function createRouterEventStream({ unstable_history: history, router, tools, }: RouterEventStreamOptions): Observable<RouterEvent>;
export {};
//# sourceMappingURL=routerEvent.d.ts.map