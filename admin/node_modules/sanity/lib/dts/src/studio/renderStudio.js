import React from 'react';
import { render } from 'react-dom';
import { Studio } from './Studio';
export function renderStudio(rootElement, config) {
    if (!rootElement) {
        throw new Error('Missing root element to mount application into');
    }
    render(React.createElement(Studio, { config: config }), rootElement);
}
//# sourceMappingURL=renderStudio.js.map