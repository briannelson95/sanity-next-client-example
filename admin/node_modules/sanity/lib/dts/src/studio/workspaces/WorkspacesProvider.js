import React, { useMemo } from 'react';
import { prepareConfig } from '../../config';
import { WorkspacesContext } from './WorkspacesContext';
export function WorkspacesProvider({ config, children }) {
    const { workspaces } = useMemo(() => prepareConfig(config), [config]);
    return React.createElement(WorkspacesContext.Provider, { value: workspaces }, children);
}
//# sourceMappingURL=WorkspacesProvider.js.map