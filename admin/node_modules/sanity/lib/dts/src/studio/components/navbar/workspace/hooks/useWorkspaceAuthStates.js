import { map } from 'rxjs/operators';
import { combineLatest } from 'rxjs';
import { createHookFromObservableFactory } from '../../../../../util';
export const useWorkspaceAuthStates = createHookFromObservableFactory((workspaces) => combineLatest(workspaces.map((workspace) => 
// eslint-disable-next-line max-nested-callbacks
workspace.auth.state.pipe(map((state) => [workspace.name, state])))).pipe(map((entries) => Object.fromEntries(entries))));
//# sourceMappingURL=useWorkspaceAuthStates.js.map