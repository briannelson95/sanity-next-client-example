import { Popover } from '@sanity/ui';
import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { useColorScheme } from '../../../colorScheme';
const ResultsPopover = styled(Popover) `
  & > div {
    min-height: 43px;
    overflow: auto;
    -webkit-overflow-scrolling: touch;
  }

  &[data-popper-reference-hidden='true'] {
    display: none;
  }
`;
export const SearchPopoverContent = forwardRef(function SearchPopoverContent(props, ref) {
    const { scheme } = useColorScheme();
    return (React.createElement(ResultsPopover, { arrow: false, constrainSize: true, matchReferenceWidth: true, placement: "bottom", portal: true, radius: 2, ref: ref, scheme: scheme, ...props }));
});
//# sourceMappingURL=SearchPopoverContent.js.map