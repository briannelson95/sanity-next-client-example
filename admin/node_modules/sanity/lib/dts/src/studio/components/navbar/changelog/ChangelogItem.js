import { Card, Stack, Label, Heading } from '@sanity/ui';
import React from 'react';
import { PortableTextContent } from './portableTextContent';
const LABELS = {
    bugfix: 'Bugfixes',
    feature: 'Feature',
};
export function ChangelogItem(props) {
    const { changeType, description, title } = props;
    const label = LABELS[changeType];
    return (React.createElement(Card, { padding: 4, shadow: 1, radius: 2 },
        React.createElement(Stack, { space: title ? 4 : 3 },
            label && React.createElement(Label, { size: 1 }, label),
            React.createElement(Stack, { space: 3 },
                title && React.createElement(Heading, { size: 1 }, title),
                description && React.createElement(PortableTextContent, { value: description })))));
}
//# sourceMappingURL=ChangelogItem.js.map