import React, { forwardRef, useMemo } from 'react';
import { StateLink, useRouterState } from '../../../../router';
export const ToolLink = forwardRef(function ToolLink(props, ref) {
    const { name, ...rest } = props;
    const routerState = useRouterState();
    const state = useMemo(() => ({ ...routerState, tool: name, [name]: undefined }), [routerState, name]);
    return React.createElement(StateLink, { state: state, ...rest, ref: ref });
});
//# sourceMappingURL=ToolLink.js.map