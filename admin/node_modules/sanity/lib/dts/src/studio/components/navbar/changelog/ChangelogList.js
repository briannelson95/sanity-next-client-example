import { Stack, Flex, Heading, Badge, Text } from '@sanity/ui';
import React from 'react';
import { ChangelogItem } from './ChangelogItem';
export function ChangelogList(props) {
    const { changeItems, isLatest, version } = props;
    const features = changeItems.filter((c) => c.changeType === 'feature');
    const bugfixes = changeItems.filter((c) => c.changeType === 'bugfix');
    const bugFixesDescription = bugfixes.map(({ description }) => description).flat();
    return (React.createElement(Stack, { space: 4 },
        React.createElement(Flex, { align: "center", gap: 2 },
            React.createElement(Heading, { size: 1 },
                "v",
                version),
            isLatest && React.createElement(Badge, { tone: "positive" }, "Latest")),
        React.createElement(Stack, { space: 3 },
            features?.map((change) => (React.createElement(ChangelogItem, { key: change.title, changeType: change.changeType, description: change?.description, title: change.title }))),
            bugFixesDescription.length > 0 && (React.createElement(ChangelogItem, { changeType: "bugfix", description: bugFixesDescription }))),
        React.createElement(Flex, { justify: "flex-end" },
            React.createElement(Text, { size: 1 },
                React.createElement("a", { href: "https://github.com/sanity-io/sanity/releases", rel: "noreferrer noopener", target: "_blank" }, "See full changelog on GitHub")))));
}
//# sourceMappingURL=ChangelogList.js.map