import { SanityLogo } from '@sanity/logos';
import { Box, Card, Flex, Heading, Stack, Text } from '@sanity/ui';
import React from 'react';
import styled from 'styled-components';
const LINKS = [
    {
        url: 'https://slack.sanity.io/',
        title: 'Community',
    },
    {
        url: 'https://www.sanity.io/docs',
        title: 'Docs',
    },
    {
        url: 'https://www.sanity.io/legal/privacy',
        title: 'Privacy',
    },
    {
        url: 'https://www.sanity.io',
        title: 'sanity.io',
    },
];
const StyledText = styled(Text) `
  a {
    color: inherit;
  }
`;
export function Layout(props) {
    const { children, footer, header } = props;
    return (React.createElement(Stack, { space: 6 },
        React.createElement(Card, { border: true, radius: 3, overflow: "auto" },
            React.createElement(Stack, null,
                typeof header === 'object' && React.createElement(Box, null, header),
                typeof header === 'string' && (React.createElement(Box, { paddingY: 4 },
                    React.createElement(Heading, { align: "center", size: 1 }, header))),
                React.createElement(Box, { paddingX: 1 },
                    React.createElement(Card, { borderTop: Boolean(header), borderBottom: Boolean(footer) }, children)),
                footer && React.createElement(Box, null, footer))),
        React.createElement(Flex, { direction: "column", gap: 4, justify: "center", align: "center" },
            React.createElement(Text, { size: 3 },
                React.createElement(SanityLogo, null)),
            React.createElement(Flex, { align: "center", gap: 2 }, LINKS.map((link, index) => (React.createElement(React.Fragment, { key: link.title },
                React.createElement(StyledText, { muted: true, size: 1 },
                    React.createElement("a", { href: link.url, target: "_blank", rel: "noopener noreferrer" }, link.title)),
                index < LINKS.length - 1 && (React.createElement(Text, { size: 1, muted: true }, "\u2022")))))))));
}
//# sourceMappingURL=Layout.js.map