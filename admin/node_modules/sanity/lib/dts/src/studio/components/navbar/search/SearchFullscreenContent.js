import { Card, Portal } from '@sanity/ui';
import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { useColorScheme } from '../../../colorScheme';
const StyledCard = styled(Card) `
  flex-direction: column;
`;
export const SearchFullscreenContent = forwardRef(function SearchFullscreenContent(props, ref) {
    const { scheme } = useColorScheme();
    return (React.createElement(Portal, null,
        React.createElement(StyledCard, { display: "flex", flex: 1, ref: ref, scheme: scheme, ...props })));
});
//# sourceMappingURL=SearchFullscreenContent.js.map