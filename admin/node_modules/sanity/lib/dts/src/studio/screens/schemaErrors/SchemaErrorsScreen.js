import { Card, Container, Heading, Stack } from '@sanity/ui';
import React, { useEffect } from 'react';
import { SchemaProblemGroups } from './SchemaProblemGroups';
import { reportWarnings } from './reportWarnings';
export function SchemaErrorsScreen({ schema }) {
    const groupsWithErrors = schema._validation?.filter((group) => group.problems.some((problem) => problem.severity === 'error')) || [];
    useEffect(() => reportWarnings(schema), [schema]);
    return (React.createElement(Card, { height: "fill", overflow: "auto", paddingY: [4, 5, 6, 7], paddingX: 4, sizing: "border" },
        React.createElement(Container, { width: 1 },
            React.createElement(Stack, { space: 5 },
                React.createElement(Heading, { as: "h1" }, "Schema errors"),
                React.createElement(SchemaProblemGroups, { problemGroups: groupsWithErrors })))));
}
//# sourceMappingURL=SchemaErrorsScreen.js.map