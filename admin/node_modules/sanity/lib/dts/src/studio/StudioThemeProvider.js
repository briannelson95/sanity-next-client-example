import React from 'react';
import { ThemeProvider, LayerProvider } from '@sanity/ui';
import { useActiveWorkspace } from './activeWorkspaceMatcher';
import { useColorScheme } from './colorScheme';
export function StudioThemeProvider({ children }) {
    const theme = useActiveWorkspace().activeWorkspace.theme;
    const { scheme } = useColorScheme();
    return (React.createElement(ThemeProvider, { scheme: scheme, theme: theme },
        React.createElement(LayerProvider, null, children)));
}
//# sourceMappingURL=StudioThemeProvider.js.map