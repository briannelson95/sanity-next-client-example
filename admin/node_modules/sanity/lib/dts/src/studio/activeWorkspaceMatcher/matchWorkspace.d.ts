import { WorkspaceLike } from '../workspaces';
interface MatchWorkspaceOptions<TWorkspace extends WorkspaceLike> {
    workspaces: TWorkspace[];
    pathname: string;
}
declare type MatchWorkspaceResult<TWorkspace extends WorkspaceLike> = {
    type: 'match';
    workspace: TWorkspace;
} | {
    type: 'redirect';
    pathname: string;
} | {
    type: 'not-found';
};
/**
 * Given a pathname and a list of workspaces, returns either a workspace match,
 * a redirect, or not-found.
 *
 * Throws if the workspace `basePaths` could result in ambiguous matches.
 */
export declare function matchWorkspace<TWorkspace extends WorkspaceLike>({ pathname, workspaces, }: MatchWorkspaceOptions<TWorkspace>): MatchWorkspaceResult<TWorkspace>;
export {};
//# sourceMappingURL=matchWorkspace.d.ts.map