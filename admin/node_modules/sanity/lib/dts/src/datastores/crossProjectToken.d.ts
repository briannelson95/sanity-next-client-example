import { Observable } from 'rxjs';
import { SanityClient } from '@sanity/client';
export interface CrossProjectTokenStore {
    getTokenDocumentId: ({ tokenId, projectId }: {
        tokenId?: string;
        projectId: string;
    }) => string;
    getProjectIdFromTokenDocumentId: (id: string) => null | string;
    fetchCrossProjectToken: (client: SanityClient, { projectId, tokenId }: {
        projectId: string;
        tokenId?: string;
    }) => Observable<string | undefined>;
    fetchAllCrossProjectTokens: () => Observable<{
        projectId: string;
        token: string;
    }[]>;
}
export interface CrossProjectTokenStoreOptions {
    client: SanityClient;
}
export declare function __tmp_wrap_crossProjectToken({ client: _client, }: CrossProjectTokenStoreOptions): CrossProjectTokenStore;
//# sourceMappingURL=crossProjectToken.d.ts.map