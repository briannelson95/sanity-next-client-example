import { Box, Code, Text } from '@sanity/ui';
import React, { useEffect, useState } from 'react';
import { usePresenceStore } from '../datastores';
export default function PresenceStory() {
    const presenceStore = usePresenceStore();
    const [globalPresence, setGlobalPresence] = useState(null);
    useEffect(() => {
        const sub = presenceStore.globalPresence$.subscribe(setGlobalPresence);
        return () => sub.unsubscribe();
    }, [presenceStore]);
    return (React.createElement(Box, { padding: 4 },
        React.createElement(Text, { size: 1, weight: "semibold" },
            React.createElement("code", null, `presenceStore.globalPresence$`)),
        React.createElement(Box, { marginTop: 3 },
            React.createElement(Code, { language: "json", size: 1 }, JSON.stringify(globalPresence, null, 2)))));
}
//# sourceMappingURL=PresenceStory.js.map