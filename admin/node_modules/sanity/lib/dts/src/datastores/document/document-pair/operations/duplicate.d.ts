import type { Observable } from 'rxjs';
import type { SanityDocument } from '@sanity/client';
import type { OperationArgs } from '../../types';
export declare const duplicate: {
    disabled: ({ snapshots }: OperationArgs) => 'NOTHING_TO_DUPLICATE' | false;
    execute: ({ client, schema, snapshots, typeName }: OperationArgs, dupeId: string) => Observable<SanityDocument>;
};
//# sourceMappingURL=duplicate.d.ts.map