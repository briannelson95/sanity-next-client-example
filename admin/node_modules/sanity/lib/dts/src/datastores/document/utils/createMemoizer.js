export function memoize(fn, keyGen) {
    const MEMO = Object.create(null);
    return (...args) => {
        const key = keyGen(...args);
        if (!(key in MEMO)) {
            MEMO[key] = fn(...args);
        }
        return MEMO[key];
    };
}
//# sourceMappingURL=createMemoizer.js.map