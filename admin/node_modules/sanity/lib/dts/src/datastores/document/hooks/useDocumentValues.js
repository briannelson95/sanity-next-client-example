import { useMemo } from 'react';
import { of } from 'rxjs';
import { useLoadable } from '../../../util';
import { useDocumentPreviewStore } from '../../datastores';
export function useDocumentValues(documentId, paths) {
    const documentPreviewStore = useDocumentPreviewStore();
    const documentValues$ = useMemo(() => documentId
        ? documentPreviewStore.observePaths({ _type: 'reference', _ref: documentId }, paths)
        : of(undefined), [documentId, documentPreviewStore, paths]);
    return useLoadable(documentValues$);
}
//# sourceMappingURL=useDocumentValues.js.map