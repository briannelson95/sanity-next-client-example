import { SanityClient } from '@sanity/client';
import { Observable } from 'rxjs';
import { ReconnectEvent, WelcomeEvent, MutationEvent } from './types';
export declare type ListenQueryParams = Record<string, string | number | boolean | string[]>;
export interface ListenQueryOptions {
    tag?: string;
    apiVersion?: string;
    throttleTime?: number;
    transitions?: ('update' | 'appear' | 'disappear')[];
}
declare const fetch: (client: SanityClient, query: string, params: ListenQueryParams, options: ListenQueryOptions) => Observable<any>;
declare const listen: (client: SanityClient, query: string, params: ListenQueryParams, options: ListenQueryOptions) => Observable<ReconnectEvent | MutationEvent | WelcomeEvent>;
export declare const listenQuery: (client: SanityClient, query: string | {
    fetch: string;
    listen: string;
}, params?: ListenQueryParams, options?: ListenQueryOptions) => Observable<any>;
export {};
//# sourceMappingURL=listenQuery.d.ts.map