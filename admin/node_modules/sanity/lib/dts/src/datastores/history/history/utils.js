export function isSameAnnotation(a, b) {
    if (a && b) {
        return a.author === b.author && a.chunk === b.chunk;
    }
    if (!a && !b) {
        return true;
    }
    return false;
}
export function getAttrs(doc) {
    return doc.draft || doc.published;
}
//# sourceMappingURL=utils.js.map