/// <reference types="react" />
import { SanityClient } from '@sanity/client';
import { Observable } from 'rxjs';
import { LoginComponentProps } from './types';
interface CreateLoginComponentOptions {
    getClient: () => Observable<SanityClient>;
    loginMethod: 'dual' | 'cookie';
    mode?: 'append' | 'replace';
    redirectOnSingle?: boolean;
    providers?: Array<{
        name: string;
        title: string;
        url: string;
        logo?: string;
    }>;
}
export declare function createLoginComponent({ getClient, loginMethod, redirectOnSingle, ...providerOptions }: CreateLoginComponentOptions): ({ projectId, basePath }: LoginComponentProps) => JSX.Element;
export {};
//# sourceMappingURL=createLoginComponent.d.ts.map