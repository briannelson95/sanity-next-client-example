import React from 'react';
import { Router, RouterState } from './types';
/**
 * @public
 */
export interface RouterProviderProps {
    onNavigate: (opts: {
        path: string;
        replace?: boolean;
    }) => void;
    router: Router;
    state: RouterState;
    children: React.ReactNode;
}
/**
 * @example
 * ```tsx
 * import {
 *   NavigateOptions,
 *   route,
 *   RouterProvider,
 *   RouterState
 * } from 'sanity'
 * import {useCallback, useMemo} from 'react'
 *
 * function Root() {
 *   const router = useMemo(() => route.create('/'), [])
 *
 *   const [state, setState] = useState<RouterState>({})
 *
 *   const handleNavigate = useCallback((
 *     path: string,
 *     options?: NavigateOptions
 *   ) => {
 *     console.log('navigate', path, options)
 *
 *     setState(router.decode(path))
 *   }, [router])
 *
 *   return (
 *     <RouterProvider
 *       onNavigate={handleNavigate}
 *       router={router}
 *       state={state}
 *     >
 *       <div>This is a routed application</div>
 *     </RouterProvider>
 *   )
 * }
 * ```
 *
 * @public
 */
export declare function RouterProvider(props: RouterProviderProps): React.ReactElement;
//# sourceMappingURL=RouterProvider.d.ts.map