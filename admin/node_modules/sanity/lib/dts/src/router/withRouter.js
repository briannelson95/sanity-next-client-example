import React from 'react';
import { useRouter } from './useRouter';
/**
 * @public
 */
export function withRouter(Component) {
    function WithRouter(props) {
        const router = useRouter();
        return React.createElement(Component, { ...props, router: router });
    }
    WithRouter.displayName = `withRouter(${Component.displayName || Component.name})`;
    return WithRouter;
}
/**
 * @public
 */
export const WithRouter = withRouter((props) => props.children(props.router));
//# sourceMappingURL=withRouter.js.map