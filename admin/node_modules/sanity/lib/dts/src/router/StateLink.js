import React, { forwardRef } from 'react';
import { useStateLink } from './useStateLink';
/**
 * @public
 */
export const StateLink = forwardRef(function StateLink(props, ref) {
    const { onClick, replace, state, target, toIndex = false, ...restProps } = props;
    const { handleClick, href } = useStateLink({ onClick, replace, state, target, toIndex });
    return React.createElement("a", { ...restProps, href: href, onClick: handleClick, ref: ref });
});
//# sourceMappingURL=StateLink.js.map