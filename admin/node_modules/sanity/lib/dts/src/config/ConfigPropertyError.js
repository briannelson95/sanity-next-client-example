import { isRecord } from '../util';
export class ConfigPropertyError extends Error {
    constructor({ propertyName, path, cause }) {
        const message = isRecord(cause) && typeof cause?.message === 'string' ? `: ${cause.message}` : '';
        super(`An error occurred while resolving \`${propertyName}\` from ${path.join(' > ')}${message}`);
        this.propertyName = propertyName;
        this.cause = cause;
        this.path = path;
    }
}
//# sourceMappingURL=ConfigPropertyError.js.map