import { Schema } from '@sanity/types';
import { Template } from './types';
export declare function validateTemplates(schema: Schema, templates: Template[]): Template[];
export declare function validateInitialObjectValue<T extends Record<string, unknown>>(value: T, template: Template): T;
//# sourceMappingURL=validate.d.ts.map