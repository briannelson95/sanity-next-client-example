import React from 'react';
import { UserColorManager } from './types';
export interface UserColorManagerProviderProps {
    children: React.ReactNode;
    manager?: UserColorManager;
}
export declare function UserColorManagerProvider({ children, manager: managerFromProps, }: UserColorManagerProviderProps): React.ReactElement;
//# sourceMappingURL=provider.d.ts.map