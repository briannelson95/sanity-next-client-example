import React, { useMemo } from 'react';
import { UserColorManagerContext } from './context';
import { createUserColorManager } from './manager';
export function UserColorManagerProvider({ children, manager: managerFromProps, }) {
    const manager = useMemo(() => {
        return managerFromProps || createUserColorManager();
    }, [managerFromProps]);
    return (React.createElement(UserColorManagerContext.Provider, { value: manager }, children));
}
//# sourceMappingURL=provider.js.map