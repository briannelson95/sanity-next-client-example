import type { SanityClient } from '@sanity/client';
import type { CurrentUser } from '@sanity/types';
import { SingleWorkspace, Source, Workspace } from '../../src/config';
export interface MockWorkspaceOptions {
    config?: Partial<SingleWorkspace>;
    client?: SanityClient;
    currentUser?: CurrentUser;
}
export declare function getMockWorkspace({ config: userConfig, currentUser, client, }?: MockWorkspaceOptions): Promise<Workspace>;
export declare function getMockSource(options?: MockWorkspaceOptions): Promise<Source>;
//# sourceMappingURL=getMockWorkspaceFromConfig.d.ts.map