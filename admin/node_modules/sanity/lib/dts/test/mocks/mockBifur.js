import { of } from 'rxjs';
export function createMockBifurClient() {
    const data = {
        requests: {
            presence: [],
        },
    };
    return {
        request: (id) => {
            return of(data.requests[id] || null);
        },
    };
}
//# sourceMappingURL=mockBifur.js.map