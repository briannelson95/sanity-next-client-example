"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useValidationStatus = useValidationStatus;

var _reactRx = require("react-rx");

var _datastores = require("../datastores");

const INITIAL = {
  validation: [],
  isValidating: false
};

function useValidationStatus(publishedDocId, docTypeName) {
  const documentStore = (0, _datastores.useDocumentStore)();
  return (0, _reactRx.useMemoObservable)(() => documentStore.pair.validation(publishedDocId, docTypeName), [publishedDocId, docTypeName], INITIAL);
}