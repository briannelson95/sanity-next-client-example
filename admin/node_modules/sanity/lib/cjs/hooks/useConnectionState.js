"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useConnectionState = useConnectionState;

var _operators = require("rxjs/operators");

var _rxjs = require("rxjs");

var _reactRx = require("react-rx");

var _datastores = require("../datastores");

const INITIAL = 'connecting';

function useConnectionState(publishedDocId, docTypeName) {
  const documentStore = (0, _datastores.useDocumentStore)();
  return (0, _reactRx.useMemoObservable)(() => documentStore.pair.documentEvents(publishedDocId, docTypeName).pipe((0, _operators.map)(ev => ev.type), (0, _operators.map)(eventType => eventType !== 'reconnect'), (0, _operators.switchMap)(isConnected => isConnected ? (0, _rxjs.of)('connected') : (0, _rxjs.timer)(200).pipe((0, _operators.mapTo)('reconnecting'))), (0, _operators.startWith)(INITIAL), (0, _operators.distinctUntilChanged)()), [publishedDocId, docTypeName], INITIAL);
}