"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useSyncState = useSyncState;

var _reactRx = require("react-rx");

var _operators = require("rxjs/operators");

var _datastores = require("../datastores");

const SYNCING = {
  isSyncing: true
};
const NOT_SYNCING = {
  isSyncing: false
};

function useSyncState(publishedDocId, documentType) {
  const documentStore = (0, _datastores.useDocumentStore)();
  return (0, _reactRx.useMemoObservable)(() => documentStore.pair.consistencyStatus(publishedDocId, documentType).pipe((0, _operators.map)(isConsistent => isConsistent ? NOT_SYNCING : SYNCING)), [publishedDocId], NOT_SYNCING);
}