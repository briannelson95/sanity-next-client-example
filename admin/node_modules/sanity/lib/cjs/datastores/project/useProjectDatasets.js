"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useProjectDatasets = useProjectDatasets;

var _react = require("react");

var _datastores = require("../datastores");

function useProjectDatasets() {
  const projectStore = (0, _datastores.useProjectStore)();
  const [value, setValue] = (0, _react.useState)(null);
  (0, _react.useEffect)(() => {
    const project$ = projectStore.getDatasets();
    const sub = project$.subscribe(setValue);
    return () => sub.unsubscribe();
  }, [projectStore]);
  return {
    value
  };
}