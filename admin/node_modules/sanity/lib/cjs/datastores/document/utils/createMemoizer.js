"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.memoize = memoize;

function memoize(fn, keyGen) {
  const MEMO = Object.create(null);
  return function () {
    const key = keyGen(...arguments);

    if (!(key in MEMO)) {
      MEMO[key] = fn(...arguments);
    }

    return MEMO[key];
  };
}