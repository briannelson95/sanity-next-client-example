"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.restore = void 0;

var _isLiveEditEnabled = require("../utils/isLiveEditEnabled");

const restore = {
  disabled: () => false,
  execute: (_ref, fromRevision) => {
    let {
      historyStore,
      idPair,
      schema,
      typeName
    } = _ref;
    const targetId = (0, _isLiveEditEnabled.isLiveEditEnabled)(schema, typeName) ? idPair.publishedId : idPair.draftId;
    return historyStore.restore(idPair.publishedId, targetId, fromRevision);
  }
};
exports.restore = restore;