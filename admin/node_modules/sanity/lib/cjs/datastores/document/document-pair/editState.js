"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.editState = void 0;

var _operators = require("rxjs/operators");

var _createMemoizer = require("../utils/createMemoizer");

var _operationArgs = require("./operationArgs");

var _isLiveEditEnabled = require("./utils/isLiveEditEnabled");

const editState = (0, _createMemoizer.memoize)((ctx, idPair, typeName) => {
  const liveEdit = (0, _isLiveEditEnabled.isLiveEditEnabled)(ctx.schema, typeName);
  return (0, _operationArgs.operationArgs)(ctx, idPair, typeName).pipe((0, _operators.map)(_ref => {
    let {
      snapshots
    } = _ref;
    return {
      id: idPair.publishedId,
      type: typeName,
      draft: snapshots.draft,
      published: snapshots.published,
      liveEdit,
      ready: true
    };
  }), (0, _operators.startWith)({
    id: idPair.publishedId,
    type: typeName,
    draft: null,
    published: null,
    liveEdit,
    ready: false
  }), (0, _operators.publishReplay)(1), (0, _operators.refCount)());
}, (_ctx, idPair, typeName) => idPair.publishedId + typeName);
exports.editState = editState;