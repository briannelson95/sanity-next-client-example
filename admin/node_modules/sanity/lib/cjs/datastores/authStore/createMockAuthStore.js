"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createMockAuthStore = createMockAuthStore;

var _rxjs = require("rxjs");

/**
 * Creates a mock `AuthStore` (for testing) that emits an `AuthState` derived
 * from the `client` and `currentUser` given.
 */
function createMockAuthStore(_ref) {
  let {
    client,
    currentUser = null
  } = _ref;
  return {
    state: (0, _rxjs.of)({
      authenticated: true,
      client,
      currentUser
    })
  };
}