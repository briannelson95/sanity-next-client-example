"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getProviderTitle = void 0;

const getProviderTitle = provider => {
  if (provider === 'google') {
    return 'Google';
  }

  if (provider === 'github') {
    return 'GitHub';
  }

  if (provider === 'sanity') {
    return 'Sanity';
  }

  if (provider !== null && provider !== void 0 && provider.startsWith('saml-')) {
    return 'SAML/SSO';
  }

  return undefined;
};

exports.getProviderTitle = getProviderTitle;