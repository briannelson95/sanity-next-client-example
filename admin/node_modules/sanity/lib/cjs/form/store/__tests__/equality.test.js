"use strict";

var _schema = _interopRequireDefault(require("@sanity/schema"));

var _formState = require("../formState");

var _shared = require("./shared");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getBookType(properties) {
  return _schema.default.compile({
    name: 'test',
    types: [{
      name: 'book',
      type: 'document',
      ...properties.root,
      fields: [{
        name: 'title',
        type: 'string',
        ...properties.title
      }, {
        name: 'subtitle',
        type: 'string',
        ...properties.subtitle
      }, {
        name: 'quotes',
        type: 'array',
        ...properties.quotes,
        of: [{
          type: 'object',
          fields: [{
            name: 'quoteText',
            type: 'string',
            ...properties.quoteText
          }, {
            name: 'pageNumber',
            type: 'number',
            ...properties.quotePageNumber
          }]
        }]
      }, {
        name: 'author',
        type: 'object',
        fields: [{
          name: 'firstName',
          title: 'First name',
          type: 'string',
          ...properties.authorFirstName
        }, {
          name: 'lastName',
          title: 'Last name',
          type: 'string',
          ...properties.authorLastName
        }],
        ...properties.author
      }]
    }]
  }).get('book');
}

test('it doesnt return new object equalities given the same input', () => {
  const document = {
    _id: 'test',
    _type: 'foo'
  };
  const bookType = getBookType({});
  const state1 = (0, _formState.prepareFormState)({ ..._shared.DEFAULT_PROPS,
    schemaType: bookType,
    document
  });
  const state2 = (0, _formState.prepareFormState)({ ..._shared.DEFAULT_PROPS,
    schemaType: bookType,
    document
  });
  expect(state1).not.toBe(null);
  expect(state2).not.toBe(null);

  if (state1 === null || state2 === null) {
    throw new Error('should not be hidden');
  }

  expect(state1 === null || state1 === void 0 ? void 0 : state1.value).toBe(state2 === null || state2 === void 0 ? void 0 : state2.value); // expect(state1.members[0]).toBe(state2.members[0])
});