"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.FileTarget = void 0;

var _ui = require("@sanity/ui");

var _fileTarget = require("../../common/fileTarget");

var _withFocusRing = require("../../../components/withFocusRing");

/* eslint-disable import/named */
// Note: FileTarget needs its own focusRing because we need show it on click, not only when :focus-visible
const FileTarget = (0, _fileTarget.fileTarget)((0, _withFocusRing.withFocusRing)(_ui.Card));
exports.FileTarget = FileTarget;