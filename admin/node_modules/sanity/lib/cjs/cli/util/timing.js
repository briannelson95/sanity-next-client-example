"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getTimer = getTimer;

var _perf_hooks = require("perf_hooks");

function getTimer() {
  const timings = {};
  const startTimes = {};

  function start(name) {
    if (typeof startTimes[name] !== 'undefined') {
      throw new Error("Timer \"".concat(name, "\" already started, cannot overwrite"));
    }

    startTimes[name] = _perf_hooks.performance.now();
  }

  function end(name) {
    if (typeof startTimes[name] === 'undefined') {
      throw new Error("Timer \"".concat(name, "\" never started, cannot end"));
    }

    timings[name] = _perf_hooks.performance.now() - startTimes[name];
    return timings[name];
  }

  return {
    start,
    end,
    getTimings: () => timings
  };
}