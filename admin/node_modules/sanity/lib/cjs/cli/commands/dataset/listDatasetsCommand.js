"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _listAliasesHandler = require("./alias/listAliasesHandler");

const listDatasetsCommand = {
  name: 'list',
  group: 'dataset',
  helpText: '',
  signature: '',
  description: 'List datasets of your project',
  action: async (args, context) => {
    const {
      apiClient,
      output
    } = context;
    const client = apiClient();
    const datasets = await client.datasets.list();
    output.print(datasets.map(set => set.name).join('\n')); // Print alias list

    await (0, _listAliasesHandler.listAliasesHandler)(args, context);
  }
};
var _default = listDatasetsCommand;
exports.default = _default;