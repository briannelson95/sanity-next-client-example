"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.promptForDatasetName = promptForDatasetName;

var _validateDatasetName = require("./validateDatasetName");

function promptForDatasetName(prompt) {
  let options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  return prompt.single({
    type: 'input',
    message: 'Dataset name:',
    validate: name => {
      const err = (0, _validateDatasetName.validateDatasetName)(name);

      if (err) {
        return err;
      }

      return true;
    },
    ...options
  });
}