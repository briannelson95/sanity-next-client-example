"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.promptForDatasetAliasName = promptForDatasetAliasName;

var _validateDatasetAliasName = require("./validateDatasetAliasName");

function promptForDatasetAliasName(prompt) {
  let options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  return prompt.single({
    type: 'input',
    message: 'Alias name:',
    validate: name => {
      const err = (0, _validateDatasetAliasName.validateDatasetAliasName)(name);

      if (err) {
        return err;
      }

      return true;
    },
    ...options
  });
}