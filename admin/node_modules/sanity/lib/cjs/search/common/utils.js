"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSearchableTypes = void 0;

var _isNonNullable = require("../../util/isNonNullable");

const isDocumentType = type => Boolean(type.type && type.type.name === 'document');

const isSanityType = type => type.name.startsWith('sanity.');

const getSearchableTypes = schema => schema.getTypeNames().map(typeName => schema.get(typeName)).filter(_isNonNullable.isNonNullable).filter(schemaType => isDocumentType(schemaType)).filter(type => !isSanityType(type));

exports.getSearchableTypes = getSearchableTypes;