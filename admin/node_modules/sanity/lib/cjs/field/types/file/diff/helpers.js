"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSizeDiff = getSizeDiff;

function getSizeDiff(prev, next) {
  if (!prev || !next) {
    return 0;
  }

  const increase = next - prev;
  const pct = Math.round(increase / prev * 100);
  return pct;
}