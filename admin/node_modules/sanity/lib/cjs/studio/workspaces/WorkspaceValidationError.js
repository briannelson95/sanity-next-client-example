"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.WorkspaceValidationError = void 0;

var _validateWorkspaces = require("./validateWorkspaces");

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

/**
 * Thrown on workspace validation errors. Includes an identifier that is either the name of
 * the workspace, or in the case of a missing or invalid name, an index and a potential title
 */
class WorkspaceValidationError extends Error {
  constructor(message, options) {
    super(message);

    _defineProperty(this, "index", void 0);

    _defineProperty(this, "identifier", void 0);

    this.index = options === null || options === void 0 ? void 0 : options.index;
    this.identifier = (options === null || options === void 0 ? void 0 : options.workspace) && (0, _validateWorkspaces.getWorkspaceIdentifier)(options.workspace, options.index);
  }

}

exports.WorkspaceValidationError = WorkspaceValidationError;