"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createRouter = createRouter;

var _router = require("../../router");

function createRouter(opts) {
  const {
    basePath = '/',
    tools
  } = opts;

  const toolRoute = _router.route.create('/:tool', toolParams => {
    const tool = tools.find(current => current.name === toolParams.tool);
    return tool ? _router.route.scope(tool.name, '/', tool.router) : _router.route.create('/');
  });

  return _router.route.create(basePath, [_router.route.intents('/intent'), toolRoute]);
}