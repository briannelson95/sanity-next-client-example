"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports._createRenderItem = _createRenderItem;

var _defaults = require("../../form/studio/defaults");

function _collectMiddleware(middlewares, plugins) {
  for (const plugin of plugins) {
    var _plugin$form;

    // Recursive
    if (plugin.plugins) {
      _collectMiddleware(middlewares, plugin.plugins);
    }

    if ((_plugin$form = plugin.form) !== null && _plugin$form !== void 0 && _plugin$form.renderItem) {
      var _plugin$form2;

      middlewares.push((_plugin$form2 = plugin.form) === null || _plugin$form2 === void 0 ? void 0 : _plugin$form2.renderItem);
    }
  }
}
/**
 * @internal
 */


function _createRenderItem(config) {
  return props => {
    var _config$form;

    const middlewares = [];

    if (config.plugins) {
      _collectMiddleware(middlewares, config.plugins);
    }

    if ((_config$form = config.form) !== null && _config$form !== void 0 && _config$form.renderItem) {
      var _config$form2;

      middlewares.push((_config$form2 = config.form) === null || _config$form2 === void 0 ? void 0 : _config$form2.renderItem);
    } // Initialize the callback chain


    let next = _defaults.defaultRenderItem;

    for (const middleware of middlewares) {
      const defaultRender = next;

      next = itemProps => {
        return middleware(itemProps, defaultRender) || defaultRender(itemProps);
      };
    }

    return next(props);
  };
}