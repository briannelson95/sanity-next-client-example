"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports._createRenderField = _createRenderField;

var _defaults = require("../../form/studio/defaults");

function _collectMiddleware(middlewares, plugins) {
  for (const plugin of plugins) {
    var _plugin$form;

    // Recursive
    if (plugin.plugins) {
      _collectMiddleware(middlewares, plugin.plugins);
    }

    if ((_plugin$form = plugin.form) !== null && _plugin$form !== void 0 && _plugin$form.renderField) {
      var _plugin$form2;

      middlewares.push((_plugin$form2 = plugin.form) === null || _plugin$form2 === void 0 ? void 0 : _plugin$form2.renderField);
    }
  }
}
/**
 * @internal
 */


function _createRenderField(config) {
  return props => {
    var _config$form;

    const middlewares = [];

    if (config.plugins) {
      _collectMiddleware(middlewares, config.plugins);
    }

    if ((_config$form = config.form) !== null && _config$form !== void 0 && _config$form.renderField) {
      var _config$form2;

      middlewares.push((_config$form2 = config.form) === null || _config$form2 === void 0 ? void 0 : _config$form2.renderField);
    } // Initialize the callback chain


    let next = _defaults.defaultRenderField;

    for (const middleware of middlewares) {
      const defaultRender = next;

      next = fieldProps => {
        return middleware(fieldProps, defaultRender) || defaultRender(fieldProps);
      };
    }

    return next(props);
  };
}