import { SanityDefaultPreview } from './SanityDefaultPreview';
export function _resolvePreviewComponent(type) {
  var _type$components;

  return ((_type$components = type.components) === null || _type$components === void 0 ? void 0 : _type$components.preview) || SanityDefaultPreview;
}