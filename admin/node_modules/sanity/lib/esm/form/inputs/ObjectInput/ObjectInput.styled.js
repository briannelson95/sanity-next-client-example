var _templateObject;

function _taggedTemplateLiteral(strings, raw) { if (!raw) { raw = strings.slice(0); } return Object.freeze(Object.defineProperties(strings, { raw: { value: Object.freeze(raw) } })); }

import { Card } from '@sanity/ui';
import styled from 'styled-components'; // The negative margins here removes the extra space between the tabs and the fields when inside of a grid

export const FieldGroupTabsWrapper = styled(Card)(_templateObject || (_templateObject = _taggedTemplateLiteral(["\n  margin-bottom: ", "px;\n  padding-bottom: ", "px;\n"])), _ref => {
  let {
    $level,
    theme
  } = _ref;
  return $level === 0 ? 0 : theme.sanity.space[5] * -1;
}, _ref2 => {
  let {
    $level,
    theme
  } = _ref2;
  return $level === 0 ? theme.sanity.space[4] : theme.sanity.space[4];
});