import * as is from '../../utils/is';
import { uploaders } from './uploaders';
import { accepts } from './accepts';
export function resolveUploader(type, file) {
  return uploaders.find(uploader => {
    var _type$options;

    return is.type(uploader.type, type) && accepts(file, uploader.accepts) && accepts(file, ((_type$options = type.options) === null || _type$options === void 0 ? void 0 : _type$options.accept) || '');
  }) || null;
}