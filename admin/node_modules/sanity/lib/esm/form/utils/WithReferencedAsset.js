import React from 'react';
import { useMemoObservable } from 'react-rx';
export function WithReferencedAsset(props) {
  const {
    reference,
    children,
    observeAsset,
    waitPlaceholder
  } = props;
  const documentId = reference === null || reference === void 0 ? void 0 : reference._ref;
  const asset = useMemoObservable(() => observeAsset(documentId), [documentId]);
  return /*#__PURE__*/React.createElement(React.Fragment, null, documentId && asset ? children(asset) : waitPlaceholder);
}