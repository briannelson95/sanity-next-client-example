import { of } from 'rxjs';

/**
 * Creates a mock `AuthStore` (for testing) that emits an `AuthState` derived
 * from the `client` and `currentUser` given.
 */
export function createMockAuthStore(_ref) {
  let {
    client,
    currentUser = null
  } = _ref;
  return {
    state: of({
      authenticated: true,
      client,
      currentUser
    })
  };
}