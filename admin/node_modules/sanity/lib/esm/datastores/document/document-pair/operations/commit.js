import { merge } from 'rxjs';
export const commit = {
  disabled: () => false,
  execute: _ref => {
    let {
      draft,
      published
    } = _ref;
    return merge(draft.commit(), published.commit());
  }
};