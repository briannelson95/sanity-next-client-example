import React, { useMemo } from 'react';
import { UserColorManagerContext } from './context';
import { createUserColorManager } from './manager';
export function UserColorManagerProvider(_ref) {
  let {
    children,
    manager: managerFromProps
  } = _ref;
  const manager = useMemo(() => {
    return managerFromProps || createUserColorManager();
  }, [managerFromProps]);
  return /*#__PURE__*/React.createElement(UserColorManagerContext.Provider, {
    value: manager
  }, children);
}