import React from 'react';
import { AvatarStack } from '@sanity/ui';
import { UserAvatar } from '../../../../components/UserAvatar';
export function UserAvatarStack(_ref) {
  let {
    maxLength,
    userIds
  } = _ref;
  return /*#__PURE__*/React.createElement(AvatarStack, {
    maxLength: maxLength
  }, userIds.map(userId => /*#__PURE__*/React.createElement(UserAvatar, {
    key: userId,
    user: userId,
    withTooltip: true
  })));
}