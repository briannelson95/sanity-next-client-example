import React from 'react';
import { Breadcrumbs, Text } from '@sanity/ui';
import { ChangeTitleSegment } from './ChangeTitleSegment';
export function ChangeBreadcrumb(props) {
  const {
    change,
    titlePath
  } = props;
  return /*#__PURE__*/React.createElement(Breadcrumbs, {
    separator: /*#__PURE__*/React.createElement(Text, {
      muted: true,
      size: 1
    }, "/")
  }, titlePath.map((titleSegment, idx) => {
    const showSegment = typeof titleSegment === 'string' || !change || change.showIndex;

    if (!showSegment) {
      return null;
    }

    return /*#__PURE__*/React.createElement(ChangeTitleSegment, {
      change: change,
      key: idx,
      segment: titleSegment
    });
  }));
}