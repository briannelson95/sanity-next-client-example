import React, { createContext, useContext } from 'react';
const WorkspaceContext = /*#__PURE__*/createContext(null);
export function WorkspaceProvider(_ref) {
  let {
    children,
    workspace
  } = _ref;
  return /*#__PURE__*/React.createElement(WorkspaceContext.Provider, {
    value: workspace
  }, children);
}
export function useWorkspace() {
  const workspace = useContext(WorkspaceContext);
  if (!workspace) throw new Error('Could not find `workspace` context');
  return workspace;
}