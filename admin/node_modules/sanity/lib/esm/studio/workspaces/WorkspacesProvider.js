import React, { useMemo } from 'react';
import { prepareConfig } from '../../config';
import { WorkspacesContext } from './WorkspacesContext';
export function WorkspacesProvider(_ref) {
  let {
    config,
    children
  } = _ref;
  const {
    workspaces
  } = useMemo(() => prepareConfig(config), [config]);
  return /*#__PURE__*/React.createElement(WorkspacesContext.Provider, {
    value: workspaces
  }, children);
}