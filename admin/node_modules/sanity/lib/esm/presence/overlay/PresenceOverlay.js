import React from 'react';
import { DISABLE_OVERLAY } from '../constants';
import { Tracker } from './tracker';
import { StickyOverlay } from './StickyOverlay';
const DEFAULT_MARGINS = [0, 0, 0, 0];

function OverlayEnabled(_ref) {
  let {
    children,
    margins
  } = _ref;
  return /*#__PURE__*/React.createElement(Tracker, null, /*#__PURE__*/React.createElement(StickyOverlay, {
    margins: margins || DEFAULT_MARGINS
  }, children));
}

export function OverlayDisabled(props) {
  return props.children;
}
export const PresenceOverlay = DISABLE_OVERLAY ? OverlayDisabled : OverlayEnabled;