function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

import { isRecord } from '../util';
export class ConfigResolutionError extends Error {
  constructor(_ref) {
    let {
      causes,
      name,
      type
    } = _ref;
    const messages = causes.filter(Boolean).map(cause => isRecord(cause) && typeof (cause === null || cause === void 0 ? void 0 : cause.message) === 'string' ? cause.message : String(cause));
    super("Could not resolve ".concat(type).concat(name ? " `".concat(name, "`") : '', ":\n").concat(messages.map(message => "\t- ".concat(message)).join('\n'), "\n\n"));

    _defineProperty(this, "name", void 0);

    _defineProperty(this, "type", void 0);

    _defineProperty(this, "causes", void 0);

    this.name = name;
    this.causes = causes;
    this.type = type;
  }

}