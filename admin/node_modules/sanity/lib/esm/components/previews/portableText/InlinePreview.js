import React from 'react';
import { getDevicePixelRatio } from 'use-device-pixel-ratio';
import { PREVIEW_MEDIA_SIZE } from '../constants';
import { renderPreviewMedia, renderPreviewNode } from '../helpers';
import { RootSpan, MediaSpan, TextSpan } from './InlinePreview.styled';
const DEFAULT_MEDIA_DIMENSIONS = { ...PREVIEW_MEDIA_SIZE.inline,
  fit: 'crop',
  aspect: 1,
  dpr: getDevicePixelRatio()
};
export function InlinePreview(props) {
  const {
    title,
    fallbackTitle = 'Untitled',
    media,
    mediaDimensions = DEFAULT_MEDIA_DIMENSIONS
  } = props;
  return /*#__PURE__*/React.createElement(RootSpan, {
    "data-testid": "inline-preview"
  }, media && /*#__PURE__*/React.createElement(MediaSpan, {
    "data-testid": "inline-preview-media"
  }, renderPreviewMedia(media, 'inline', mediaDimensions), /*#__PURE__*/React.createElement("span", null)), /*#__PURE__*/React.createElement(TextSpan, {
    "data-testid": "inline-preview-title",
    size: 1
  }, renderPreviewNode(title, 'inline', fallbackTitle)));
}