import { createContext } from 'react';
import { defaults } from './defaults';
/**
 * @todo: Rename to `ZOffsetsContext`
 *
 * @internal
 */

export const ZIndexContext = /*#__PURE__*/createContext(defaults);