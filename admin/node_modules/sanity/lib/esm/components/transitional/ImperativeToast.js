import { useToast } from '@sanity/ui';
import { forwardRef, useImperativeHandle } from 'react';
export const ImperativeToast = /*#__PURE__*/forwardRef((_, ref) => {
  const {
    push
  } = useToast();
  useImperativeHandle(ref, () => ({
    push
  }));
  return null;
});
ImperativeToast.displayName = 'ImperativeToast';