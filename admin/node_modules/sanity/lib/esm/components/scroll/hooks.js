import React from 'react';
import { ScrollContext } from './scrollContext';
export function useOnScroll(callback) {
  const parentContext = React.useContext(ScrollContext);
  React.useEffect(() => {
    return parentContext === null || parentContext === void 0 ? void 0 : parentContext.subscribe(callback);
  }, [callback]);
}