import React from 'react';
import { useOnScroll } from './hooks';
export function ScrollMonitor(_ref) {
  let {
    onScroll,
    children
  } = _ref;
  useOnScroll(onScroll);
  return /*#__PURE__*/React.createElement(React.Fragment, null, children);
}