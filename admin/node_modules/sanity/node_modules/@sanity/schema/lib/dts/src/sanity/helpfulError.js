import { generateHelpUrl } from '@sanity/generate-help-url';
export function createHelpfulError(message, slug) {
    return new Error(`${message}. For more info see ${generateHelpUrl(slug)}`);
}
//# sourceMappingURL=helpfulError.js.map