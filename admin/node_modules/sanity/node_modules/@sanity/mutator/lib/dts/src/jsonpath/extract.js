import { extractAccessors } from './extractAccessors';
export function extract(path, value) {
    const accessors = extractAccessors(path, value);
    return accessors.map((acc) => acc.get());
}
//# sourceMappingURL=extract.js.map