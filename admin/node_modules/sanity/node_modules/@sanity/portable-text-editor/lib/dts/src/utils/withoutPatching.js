export const PATCHING = new WeakMap();
export function withoutPatching(editor, fn) {
    const prev = isPatching(editor);
    PATCHING.set(editor, false);
    fn();
    PATCHING.set(editor, prev);
}
export function isPatching(editor) {
    return PATCHING.get(editor);
}
//# sourceMappingURL=withoutPatching.js.map