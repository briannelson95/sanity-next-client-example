"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getUserConfig = void 0;

var _configstore = _interopRequireDefault(require("configstore"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// eslint-disable-next-line no-process-env
const sanityEnv = (process.env.SANITY_INTERNAL_ENV || '').toLowerCase();
const configName = sanityEnv && sanityEnv !== 'production' ? "sanity-".concat(sanityEnv) : 'sanity';
const defaults = {};
let config;

const getUserConfig = () => {
  if (!config) {
    config = new _configstore.default(configName, defaults, {
      globalConfigPath: true
    });
  }

  return config;
};

exports.getUserConfig = getUserConfig;