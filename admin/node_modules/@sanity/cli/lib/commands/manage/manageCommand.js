"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _open = _interopRequireDefault(require("open"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const manageCommand = {
  name: 'manage',
  signature: 'manage',
  helpText: '',
  description: 'Opens the Sanity project management UI',

  async action(args, context) {
    var _cliConfig$api;

    const {
      output,
      cliConfig
    } = context;
    const {
      print
    } = output;
    const projectId = cliConfig === null || cliConfig === void 0 ? void 0 : (_cliConfig$api = cliConfig.api) === null || _cliConfig$api === void 0 ? void 0 : _cliConfig$api.projectId;
    const url = projectId ? "https://manage.sanity.io/projects/".concat(projectId) : 'https://manage.sanity.io/';
    print("Opening ".concat(url));
    await (0, _open.default)(url);
  }

};
var _default = manageCommand;
exports.default = _default;