import inquirer, { Answers, DistinctQuestion } from 'inquirer';
export declare function prompt<T extends Answers = Answers>(questions: DistinctQuestion<T>[]): Promise<T>;
export declare namespace prompt {
    var Separator: typeof import("inquirer/lib/objects/separator");
    var single: <T = string>(question: inquirer.DistinctQuestion<inquirer.Answers>) => Promise<T>;
}
//# sourceMappingURL=cliPrompter.d.ts.map