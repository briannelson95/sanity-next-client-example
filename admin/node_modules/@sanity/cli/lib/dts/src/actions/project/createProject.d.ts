import type { CliApiClient } from '../../types';
export interface CreateProjectOptions {
    displayName: string;
    organizationId?: string;
    subscription?: {
        planId: string;
    };
    metadata?: {
        coupon?: string;
    };
}
export declare function createProject(apiClient: CliApiClient, options: CreateProjectOptions): Promise<{
    projectId: string;
    displayName: string;
}>;
//# sourceMappingURL=createProject.d.ts.map