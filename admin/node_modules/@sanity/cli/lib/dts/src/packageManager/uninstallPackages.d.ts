import type { CliCommandContext } from '../types';
import { PackageManager } from './packageManagerChoice';
export interface UninstallOptions {
    packageManager: PackageManager;
    packages: string[];
}
export declare function uninstallPackages(options: UninstallOptions, context: Pick<CliCommandContext, 'output' | 'workDir'>): Promise<void>;
//# sourceMappingURL=uninstallPackages.d.ts.map