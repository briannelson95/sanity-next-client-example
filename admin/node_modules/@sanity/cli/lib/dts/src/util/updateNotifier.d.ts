import type { PackageJson } from '../types';
interface UpdateCheckOptions {
    pkg: PackageJson;
    cwd: string;
    workDir: string;
}
export declare function runUpdateCheck(options: UpdateCheckOptions): {
    notify: () => Promise<void>;
};
export {};
//# sourceMappingURL=updateNotifier.d.ts.map