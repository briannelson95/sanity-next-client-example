import minimist from 'minimist';
export function parseArguments(argv = process.argv) {
    /* eslint-disable id-length */
    // prettier-ignore
    const { _, h, help, d, debug, v, version, '--': extraArguments, ...extOptions } = minimist(argv.slice(2), {
        '--': true,
        boolean: ['h', 'help', 'd', 'debug', 'v', 'version']
    });
    const [groupOrCommand, ...argsWithoutOptions] = _;
    return {
        groupOrCommand,
        argv,
        extOptions,
        argsWithoutOptions,
        extraArguments: extraArguments || [],
        // prettier-ignore
        coreOptions: {
            h, help,
            d, debug,
            v, version,
        },
    };
    /* eslint-enable id-length */
}
//# sourceMappingURL=parseArguments.js.map