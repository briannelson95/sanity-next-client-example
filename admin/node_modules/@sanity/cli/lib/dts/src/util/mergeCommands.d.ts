import type { CliCommandDefinition, CliCommandGroupDefinition } from '../types';
export interface MergeOptions {
    cwd: string;
    workDir: string;
    cliVersion: string;
}
export declare function mergeCommands(baseCommands: (CliCommandDefinition | CliCommandGroupDefinition)[], corePath: string | undefined, options: MergeOptions): Promise<(CliCommandDefinition | CliCommandGroupDefinition)[]>;
//# sourceMappingURL=mergeCommands.d.ts.map